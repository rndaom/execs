# Native zoom, reflow, contrast and profile round-trip follow-up

September 22, 2026. Product commits **6ff64004c4366084d997dba25a9076f61f46fb71** (native zoom/reflow) and **c006dda154ddae164ed540beb9df40c28a1d80e6** (Files scroll retention) are pushed to [draft PR #60](https://github.com/rndaom/execs/pull/60). Foundry remains the sole selected direction. No version bump, tag, installer publication or release is authorized by this checkpoint.

## Changes and observed results

| Area | Result and evidence |
| --- | --- |
| Native keyboard zoom | Windows initially ignored zoom shortcuts. The main window now enables them, with a main-window-only webview zoom permission for Tauri's Linux handler. The [Windows follow-up](implementation/native-windows/zoom-follow-up/README.md) distinguishes the pre-fix 7D executable from E093: five actual Ctrl+KP_Add increments enlarged the interface, layout reflowed at 1200×800 and 960×640 client sizes, keyboard navigation reached scrolled controls, and Ctrl+0 restored normal size. Approximately 2× is a visual description; an exact engine zoom factor and OS text scaling were not measured. |
| Enlarged workspaces | The [Chrome reflow audit](implementation/zoom-reflow/reflow-qa.md) covers Comfig, Binds, Files and Launch at 600×400 and 480×320 CSS pixels. It fixes root scrolling caused by hidden labels, menus growing below the viewport, StrictMode Escape focus loss and a clipped pending-review button. Drafts and keyboard access survived the exercised navigation and resizing. These browser results do not establish native behavior for those active panes. |
| Failed preference save | A real exclusive file lock caused the native save to fail. The old settings bytes stayed exact, Retry saved the requested value, and that value survived normal close/reopen. The same [Windows report](implementation/native-windows/zoom-follow-up/README.md) preserves this E093 evidence and its historical error-copy defect. The Rust fix in 6ff6400 removes the unrelated profile-library wording while retaining the OS reason. A separate rebuilt native executable, **7D3BBA6B25B6FEAD1EB0CD1E63E34EFA93BE5E2F46974F355987F847BC53817D**, passed the actual failure-copy/Retry check: failed bytes exact, startup preference saved, error cleared and normal exit 0. This later case did not repeat zoom or restart testing. |
| Native profile import/export/restart | The [round-trip report](implementation/native-windows/import-export-follow-up/README.md) records the actual public-v0.1.8 synthetic ZIP imported by E093 with an explicit second-HUD choice. All ten imported and ten exported payloads match; the original two profiles and synthetic installed files are preserved, apart from the expected library index addition. Three profiles remain inactive and all 31 post-export files remain exact after normal close/reopen. That run used the default export filename despite the UIA value change; its documented same-case relocation preserves the exact ZIP. A separate 7D3 case subsequently entered the full path with real keyboard input and saved at that exact path: four exported payloads match, all 20 original files including the index remain exact, and the app exits normally. The earlier mismatch remains recorded. |
| Pane and editor scroll | The [Sounds → Mods → Files audit](implementation/pane-scroll-follow-up/README.md) found that hiding Files reset its internal editor scroll while its draft and outer pane position survived. A before-mutation capture and CodeMirror scroll snapshot correct this. Final browser sequences preserve all three pane offsets at 1200×800 and 960×640, the editor's measured 1009/1129-pixel offsets, all 2,470 draft bytes, backward selection and active navigation visibility on resize. These are actual browser fixture observations, not native active-profile checks. |
| Rendered text contrast | The [computed-style audit](implementation/contrast-follow-up/README.md) measures 27 meaningful text pairs across HUD, Files, import, App settings and running/deferred states. All exceed 4.5:1; the minimum is 5.4247:1. Real ancestor alpha layers and Oklab colors are composited, including the running banner and preference error. Nine screenshots, raw observations and a reproducible calculation accompany the result. No contrast CSS change was needed; unmeasured states and native rendering remain outside this scope. |
| Process scheduling | During the 7D3 inactive export session, a read-only observation records Normal process priority and 46 threads with Normal priority level. This single foreground-targeted app-process snapshot does not measure WebView2 child priorities, background behavior or TF2 frame presentation; RND-325 remains unclassified. |
| Startup failure variants | Four additional [native Windows cases](implementation/native-windows/startup-variants/README.md) exercise unset APPDATA, a mismatched maintenance token, multiple active markers and a marker held with `FileShare.None`. All show the expected OS-native diagnostic, version and copy/report guidance, preserve complete paths in Ctrl+C output and exit normally on OK. No main window, console or WebView2 process is observed. Each case retains its original settings and marker bytes for comparison; the read-denial handle stays held until the app exits. These are the exact 7D3/6ff6400 executable, not the later Files build or an installer. |
| Windows active-workspace isolation | The [source analysis](implementation/native-windows/active-fixture-analysis.md) explains why private APPDATA alone does not isolate Windows Steam Cloud. The accepted native cases remain rootless or inactive, with no game launch, profile activation, Cloud acknowledgement or player-library writes. |
| Linux native runtime | The [accepted retry](implementation/linux-native/retry-35763403785.md) passes native launch, six-profile navigation, five genuine zoom increments to 200%, keyboard menu traversal, reset, preference save/restart and fixture preservation on Ubuntu 22.04/Xvfb/WebKitGTK 2.50.4. All five settled screenshots were inspected. Its exact binary is unchanged from the first run; harness fixes distinguish a demonstrated WebDriver coordinate mismatch at 200% from application behavior. Normal-scale pointer input passes; physical Linux pointer input at 200%, Wayland and hardware rendering remain unqualified. The first run's stale images remain rejected. |

The same bounded Linux smoke also passes on the latest product **c006dda** in [run 35764804171](https://github.com/rndaom/execs/actions/runs/35764804171), tested through merge **b321d3d0aacc99ab16777cc3c9670b1310f28907**. Its distinct ELF SHA-256 is **f2cda2ec6f81957c9bd4ae2801949d89e72155769565f1c28a61838f9f0288e7**. All five screenshots were separately inspected; native preference restart and all preservation stages passed. The same directly observed driver-coordinate limitation remains. These current-product results do not turn the inactive fixture into an active Files test.

## Integrated checks

At 6ff6400, `pnpm test` passes **877 desktop tests / 103 files**, **160 cfglint / 10 files** and **43 tooling tests**, with three existing platform skips: **1,080 passed, zero failures**. TypeScript passes. `pnpm check` passes **476 files** after formatting three evidence JSON files. The new native import validator has **17 passing data-only tests**; they validate fixture boundaries and expected mutations, not GUI execution.

After the Files correction and named App settings failure fixture, c006dda passes the complete local suite: **880 desktop tests / 104 files**, **160 cfglint / 10 files**, and **45 tooling tests**: **1,085 passed, zero failures, three platform skips**. TypeScript and the production frontend build pass. The final **563-file** formatting check includes the later native metadata. Independent review found no actionable Files lifecycle/isolation defect. The fixture's two tests prove failed-save preservation and Retry without changing production behavior. Startup metadata separately passes exact-byte, full-diagnostic and archive provenance checks.

The Windows no-bundle production build at 6ff6400 passed in **3m16s**, producing the 23,442,432-byte 7D3BBA6 executable above. Frontend TypeScript/Vite passed; the existing Vite chunk-size advisory remains.

Final release-plan review found that the development changelog omitted the already published **0.1.8** and **0.1.7+2** headings. Freezing its notes for 0.2.0 would therefore select 0.1.7 as the previous release and fail the tagged-0.1.8 package fixture boundary. The published sections are restored verbatim from immutable v0.1.8, with only their five duplicate shipped bullets removed from Unreleased. A behavioral regression uses the actual repository history and the fixture's exporter tag; it fails before the correction and passes afterward. All tooling files then pass **46 tests**, with three existing platform skips: combined with the unchanged 880 desktop and 160 cfglint passes, **1,086 checks pass**. This repair changes no product version or published tag and performs no release.

The [five-job CI run at c006dda](https://github.com/rndaom/execs/actions/runs/35764804146) completed successfully on Windows and Linux, including frontend, both Rust jobs and both Inventory helper jobs. Its last job completed at 18:16:35 UTC. The [earlier 6ff6400 run](https://github.com/rndaom/execs/actions/runs/35761390677) also passed and remains attributed to that earlier product head. Linux native smoke has its separately inspected candidate and rendering limits.

The [implementation gallery](implementation/review.html#follow-up-evidence) keeps its **85 primary captures across 15 flows and five Foundry references**. New zoom/reflow and native captures are supplementary and individually attributed. Historical, rejected and pre-fix captures remain labeled in their reports.

## Acceptance still open

All 14 selected issues remain In Progress; 25 optional candidates remain unselected, and Inventory remains account-owned, read-only and development-only. The [acceptance audit](follow-up-acceptance-audit.md) retains original requirements and identifies the remaining work. This checkpoint does not replace installer/updater qualification, active native draft/write behavior, complete accessibility evidence, Steam Cloud acknowledgement, retail TF2/Casual/Restore checks, PresentMon measurements or RND-324's unresolved engine behavior with browser or fixture evidence.
