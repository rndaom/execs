# Linux native development smoke: run 35780701908

This [hosted run](https://github.com/rndaom/execs/actions/runs/35780701908) tested PR head `ee0a8f22db5f6a07af053bd070490d55c9f271e9` through merge `3b979c6cdbcf0406e8c5cdc691150f2917cc70b3`. It is development qualification of a native Linux build, not a packaged installer, signed update, Steam Cloud, or retail TF2 result. The two isolated cases have different outcomes.

The **inactive-profile case passed**: the six-profile menu, keyboard zoom interaction, and application settings persistence across a native restart were observed. Its [structured result](linux-native-smoke-3b979c6cdbcf0406e8c5cdc691150f2917cc70b3/execs-linux-native-zPfX8z/evidence/results.gen.json) and five captures are retained.

The **active Files case failed overall** at the Discard close action. Before that stop, the native session retained an unsaved Files draft and its editor position through pane navigation; explicit Save wrote the checked 4,881-byte content. A later 4,927-byte draft remained in memory when a native close request opened the review dialog. Cancel kept that draft and left the saved checkpoint unchanged. A second close request showed the Discard dialog, but its click returned an empty WebDriver `unknown error`, which the harness did not accept as proof of process exit. The [structured result](linux-native-smoke-3b979c6cdbcf0406e8c5cdc691150f2917cc70b3/execs-linux-native-active-CnuZAL/evidence/results.gen.json) and six captures document the observed steps. Discard completion, process exit, restart after Discard, and Save-and-close remain unverified by this run. The Discard capture shows the dialog **before** the click.

The original workflow artifact is API artifact `10718401519`, named `linux-native-smoke-3b979c6cdbcf0406e8c5cdc691150f2917cc70b3`. Its 18 extracted files are archived byte-for-byte beneath this directory; [provenance.json](provenance.json) records their original sizes and SHA-256 hashes. The archived generated JSON and driver logs retain their original bytes, including line endings and any trailing whitespace. The [review gallery](../../review.html) links selected captures and states the same limits.
