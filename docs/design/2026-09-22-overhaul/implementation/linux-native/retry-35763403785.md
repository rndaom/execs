# Linux native retry: passed for the bounded scope

September 22, 2026. [Workflow 35763403785](https://github.com/rndaom/execs/actions/runs/35763403785), [native-webkitgtk job 106866783072](https://github.com/rndaom/execs/actions/runs/35763403785/job/106866783072), completed successfully at **18:01:47 UTC**. The native step passed at 18:01:24. PR head `0687af0e44fed7d1f70bc71b1acbe2475ce897ff` was tested through merge revision `9522bd3e1d61f2f10c7e95228a1f12b4ef28a999`.

The no-bundle Linux ELF has SHA-256 `4d99ade0ca2686045014ae8d03efd6a33b7961c4989e7ec92eb0261a3c5d7ad1`, identical to the first run. The retry changed the test harness and evidence collection, not the product binary. The environment was GitHub-hosted Ubuntu 22.04, X11/Xvfb, software rendering, WebKitGTK/driver `2.50.4-0ubuntu0.22.04.1`, external `tauri-driver` 2.0.6 and `wry` 0.55.1. Native content used `tauri://localhost` throughout.

## Accepted results

The complete [native result and passive input trace](run-35763403785/linux-native-smoke-9522bd3e1d61f2f10c7e95228a1f12b4ef28a999/execs-linux-native-0Z84WB/evidence/results.gen.json) records these outcomes:

- **Native launch:** the confirmed synthetic install and six inactive profiles loaded at 1200×800. The runtime was real Tauri and the content was the bundled production frontend.
- **Minimum-size pointer/menu:** Choose profile opened the six-profile library at 960×640. The overlay bounds were x151–581, y111.5–545.1875; its final action was unobstructed. Escape restored the summary.
- **Actual keyboard zoom:** successive Ctrl+= inputs changed CSS content from 960×640 through 799×533, 685×457, 600×400 and 533×355 to **480×320**. The physical WebDriver screenshot remained 960×640. This is native 200% page zoom, not a resized-browser approximation.
- **Enlarged keyboard/menu:** real Tab/Enter opened the library. Tab traversal reached the name field, Import and Change install. The latter's visible focused rectangle was x153.72–299.29, y247.70–287.20 within 480×320; native hit testing passed. Escape restored the summary and Ctrl+0 restored 960×640.
- **Real native settings persistence:** the UI reported the isolated app-data directory. Clicking Reduce changed the real settings file, the displayed selection and the document preference. A new native process reloaded Reduce and showed the selected control at 1200×800.
- **Preservation:** all six profiles remained inactive; library metadata/payloads and synthetic live bytes passed comparison before launch, after the preference write and after native exit. The only accepted settings mutation was `motion: system → reduce`; startup update checking stayed off. No additive metadata changes were observed.

## Pointer limitation, directly observed

At 200% zoom the intended Choose profile button was stable and hit-testable at x177.359375–302.6328125, y64.5–102 CSS pixels. Its center was approximately (240, 83.25). WebDriver's element click instead delivered trusted `pointerdown` and `click` events to **MAIN at (119, 41)**. The button's rectangle did not move. The passive observer recorded the events and was removed immediately; it did not synthesize input or invoke application commands.

This establishes a coordinate mismatch in the tested WebDriver input path. It does not establish that a correctly positioned physical mouse click fails in execs. The same enlarged menu was successfully opened and traversed with genuine keyboard input. Normal-scale pointer input passed. **Physical Linux pointer use at 200% remains outside this result**, as do other distributions/rendering backends. The passing status is scoped accordingly rather than masking an on-target app failure.

## Accepted visual evidence

All five PNGs were inspected individually. Each now contains the expected settled UI, and every capture reports three identical native frames after fonts/animations and paint boundaries settled. The first run's pre-paint/stale images remain rejected and are not substituted into this result.

| Capture | Actual CSS / PNG size | Inspection |
| --- | --- | --- |
| [01 native inactive](run-35763403785/linux-native-smoke-9522bd3e1d61f2f10c7e95228a1f12b4ef28a999/execs-linux-native-0Z84WB/evidence/01-native-inactive-1200.png) | 1200×800 / 1200×800 | Complete inactive shell, header, Choose profile decision and footer. |
| [02 native profile menu](run-35763403785/linux-native-smoke-9522bd3e1d61f2f10c7e95228a1f12b4ef28a999/execs-linux-native-0Z84WB/evidence/02-native-menu-960.png) | 960×640 / 960×640 | Six-profile count; scrolling list, name field and Import/Change install fit. |
| [03 enlarged keyboard menu](run-35763403785/linux-native-smoke-9522bd3e1d61f2f10c7e95228a1f12b4ef28a999/execs-linux-native-0Z84WB/evidence/03-native-keyboard-zoom-menu.png) | 480×320 / 960×640 | Menu scrolled by native keyboard traversal; complete Change install focus outline and action visible. |
| [04 native preference saved](run-35763403785/linux-native-smoke-9522bd3e1d61f2f10c7e95228a1f12b4ef28a999/execs-linux-native-0Z84WB/evidence/04-native-preferences-saved.png) | 960×640 / 960×640 | Reduce selected; save confirmation; genuine isolated install/app-data locations. |
| [05 preference after restart](run-35763403785/linux-native-smoke-9522bd3e1d61f2f10c7e95228a1f12b4ef28a999/execs-linux-native-0Z84WB/evidence/05-native-preferences-after-restart.png) | 1200×800 / 1200×800 | New native process retains Reduce; Foundry settings sections render fully through Support. |

These capture the native webview content; they are not screenshots of external OS dialogs or window decorations. The observed hierarchy, warm surfaces, copper primary/focus treatment and compact hairlines are consistent with the implemented Foundry system. No new visual defect was found within these inspected states.

## Retained evidence and limits

The downloaded fixture baseline and results retain their exact artifact bytes, renamed to `.gen.json` only to exclude generated evidence from the repository formatter. Before/after SHA-256 was checked during each rename. The trace is part of the result JSON and is not summarized away. PNGs were not altered.

The workflow's **13 harness tests passed on Linux**. The prepared patch previously passed **24 local tests** including the shared fixture suite and targeted Biome. This report required no new product edit or repeated test run. It creates no installer, tag or public release and does not launch Steam or TF2.

The bounded Linux native gap is closed for launch, global preferences/persistence, inactive profile navigation, keyboard zoom/reset and the inspected reflow. This does not replace installer/updater qualification or establish profile switching/export, active gameplay writes, all panes, sound, game behavior, screen-reader operation, Wayland or hardware-rendered behavior. Those remain covered, or explicitly limited, by their separate evidence.

## Current product confirmation

The later [workflow 35764804171](https://github.com/rndaom/execs/actions/runs/35764804171), [native-webkitgtk job 106871490347](https://github.com/rndaom/execs/actions/runs/35764804171/job/106871490347), passed on September 22, 2026 at **18:11:04 UTC**. Its native step ran from 18:10:48 to 18:10:58. This is a separate current-product qualification; the earlier `0687af0` result above remains unchanged.

| Identity | Current run |
| --- | --- |
| PR head | `c006dda154ddae164ed540beb9df40c28a1d80e6` |
| Actual tested merge revision | `b321d3d0aacc99ab16777cc3c9670b1310f28907` |
| No-bundle Linux binary SHA-256 | `f2cda2ec6f81957c9bd4ae2801949d89e72155769565f1c28a61838f9f0288e7` |
| Isolated runner directory | `/home/runner/work/_temp/execs-linux-native-JudcgZ/` |
| Native content and runtime | `tauri://localhost`, `wry` 0.55.1, Linux |

The current binary differs from the historical retry. Its hash and actual source revision come from the [complete native result](run-35764804171/linux-native-smoke-b321d3d0aacc99ab16777cc3c9670b1310f28907/execs-linux-native-JudcgZ/evidence/results.gen.json), rather than assuming that the PR head itself was built. The workflow still retains fixture evidence only, not a downloadable executable.

The result confirms native launch at 1200×800; a contained six-profile menu at 960×640; genuine Ctrl+= inputs reducing the CSS viewport through 799×533, 685×457, 600×400 and 533×355 to 480×320; and keyboard access to the enlarged menu's final Change install action. At 480×320 the panel spans x34–464 and y16–304. The focused final action spans x153.72–299.29 and y247.70–287.20, with successful hit testing. Escape and Ctrl+0 restore the summary and 960×640 content dimensions.

The passive pointer trace again records a stable, hit-testable Choose profile rectangle at x177.359375–302.6328125 and y64.5–102. WebDriver's element click delivers trusted `pointerdown` and `click` events to MAIN at (119, 41), outside that rectangle, while the before/after rectangle remains identical. The classification is **driver-coordinate-mismatch**. Actual Tab/Enter input then opens and traverses the menu. This repeats the specific driver limitation; it neither qualifies physical Linux pointer use at 200% nor demonstrates a product mouse failure. Normal-scale pointer behavior passed.

The rendered App settings path matches the current isolated directory. Selecting Reduce changes the real native settings file and root preference; a new native process reloads that selection. All three preservation stages (`before-launch`, `after-ui-save`, `after-native-exit`) pass: six profiles, null active profile, preserved library and synthetic live payloads, and no additive metadata changes. The only allowed settings change is `motion: system → reduce`; the seeded startup update preference remains off.

All five current-run PNGs were opened and visually inspected individually. Each contains the expected settled state and records three identical frames; no new visual defect was found in these states. Screenshots retain their original artifact bytes.

| Current capture | Actual CSS / PNG size | Inspection |
| --- | --- | --- |
| [01 native inactive](run-35764804171/linux-native-smoke-b321d3d0aacc99ab16777cc3c9670b1310f28907/execs-linux-native-JudcgZ/evidence/01-native-inactive-1200.png) | 1200×800 / 1200×800 | Complete inactive shell, Choose profile decision and footer. |
| [02 native profile menu](run-35764804171/linux-native-smoke-b321d3d0aacc99ab16777cc3c9670b1310f28907/execs-linux-native-JudcgZ/evidence/02-native-menu-960.png) | 960×640 / 960×640 | Six-profile count; scrolling list and all footer actions fit. |
| [03 enlarged keyboard menu](run-35764804171/linux-native-smoke-b321d3d0aacc99ab16777cc3c9670b1310f28907/execs-linux-native-JudcgZ/evidence/03-native-keyboard-zoom-menu.png) | 480×320 / 960×640 | Naturally scrolled list; Change install and its complete focus outline remain visible. |
| [04 native preference saved](run-35764804171/linux-native-smoke-b321d3d0aacc99ab16777cc3c9670b1310f28907/execs-linux-native-JudcgZ/evidence/04-native-preferences-saved.png) | 960×640 / 960×640 | Reduce selected with save confirmation and current isolated locations. |
| [05 preference after restart](run-35764804171/linux-native-smoke-b321d3d0aacc99ab16777cc3c9670b1310f28907/execs-linux-native-JudcgZ/evidence/05-native-preferences-after-restart.png) | 1200×800 / 1200×800 | New native process retains Reduce; settings render through Support. |

The current artifact's `results.json` and `fixture-baseline.json` were renamed to `.gen.json` for the same formatter exclusion as the historical records. SHA-256 before and after each rename matched: results `33c207ec8e1158ab4a13a4d8101cf2fa9408767d4b2290247d0f0982416d8fdf`; [fixture baseline](run-35764804171/linux-native-smoke-b321d3d0aacc99ab16777cc3c9670b1310f28907/execs-linux-native-JudcgZ/evidence/fixture-baseline.gen.json) `fd1fb1f2ceffd5372f9599b551493fa34790a5b55afccef19ae8e54d8cb445a5`.

This confirmation archived an already-completed job and inspected its outputs. It did not rerun CI, edit product or harness code, build an installer, publish a release, or launch Steam/TF2. The bounded X11/Xvfb scope and all limitations recorded above continue to apply.
