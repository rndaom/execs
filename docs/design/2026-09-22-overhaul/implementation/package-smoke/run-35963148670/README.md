# Linux package dependency rerun: 35963148670

The [hosted run](https://github.com/rndaom/execs/actions/runs/35963148670) passed on draft PR #60 head `b6a3ca8020bc2eeec5299d0ff6171f99c56ebeba` through tested merge `496b0ee5f8e99632ddbd75c22d55c7e60e8f128b`. It built unsigned 0.2.0 development AppImage and Debian packages against the authenticated public v0.1.8 baseline. No release or signed updater was produced.

The new [dependency inspection step](https://github.com/rndaom/execs/actions/runs/35963148670/job/107515907120) passed. Its `readelf` output lists 17 direct ELF `NEEDED` libraries, including `libssl.so.3`, `libcrypto.so.3`, `libz.so.1`, `libwebkit2gtk-4.1.so.0` and `libgtk-3.so.0`; `ldd` resolved every entry on the Ubuntu 22.04 builder. The candidate Debian package's generated `Depends` field was exactly `libssl3 | libssl3t64, zlib1g, libwebkit2gtk-4.1-0, libgtk-3-0`.

Extraction of the candidate AppImage found `usr/lib/libssl.so.3` and `usr/lib/libcrypto.so.3`, with no bundled `libz.so.1`. Given the inspected build-tree ELF's direct zlib dependency, we infer that the passing Ubuntu run uses host zlib. This follows [AppImage's documented base-library approach](https://docs.appimage.org/introduction/concepts.html), whose [curated excludelist](https://github.com/AppImageCommunity/pkg2appimage/blob/master/excludelist) includes `libz.so.1`; it does not establish startup on distributions beyond this runner. The receipt also confirms `DEPENDENCIES.txt` is present and the checked conflicting `libwayland-client`, `server`, `cursor` and `egl` families are absent from the image.

All three artifact cases passed: AppImage replacement from public v0.1.8, Debian upgrade from v0.1.8, and Debian first install. Both public packages passed signature verification. The upgrade cases exported through the old native UI, imported and switched under the candidate, and reopened with the selected cfg intact; each old export matched SHA-256 `6f93d339d2c9cfdafaad7f3c754fbbe74b87a1023cd832a70802b429bc261bd2` with four payloads verified. The first-install case launched and reopened with its authored setup. All **14/15/8** case preservation checkpoints retained the original profile and payload hashes, avoided Steam discovery paths, and ended with passing final fixture comparisons. The three final Files captures were legible. Headless driver logs include transient connection and AT-SPI warnings and one GTK event assertion; no completed UI or fixture assertion failed.

The [workflow artifact](https://github.com/rndaom/execs/actions/runs/35963148670/artifacts/10793352841) contains 43 fixture-only files; the upload log reports ZIP SHA-256 `584ea437f242713dc6d98e0bbbd1a54ce5b1e95a0970e4e995bb9b4b7dc6328d`. Its 26 screenshots, eight driver logs, seven JSON receipts and two synthetic export ZIPs were reviewed for public exposure: observed paths and URLs were ephemeral `/home/runner` locations and public release links, with no tokens, personal account names, Steam IDs, personal files or retail TF2 assets. This note records the new dependency evidence without duplicating the [previous run's archived UI evidence](../run-35960303004/README.md). The job log and short-lived artifact remain the sources for exact bytes; neither candidate installer nor generated notice contents are archived here. Signed self-update, Windows NSIS, live CDN redirects, retail TF2, Steam Cloud, media decoding, Wayland and other Linux distributions remain outside this run.
