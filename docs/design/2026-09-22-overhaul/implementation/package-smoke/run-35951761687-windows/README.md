# Windows previous-public capability: run 35951761687

The [hosted run](https://github.com/rndaom/execs/actions/runs/35951761687) **failed** on PR head `065f04cf1f0c528144d3373552dc1089b2e9928c` through tested merge `04e83db6a3ec0e28bc27b9ada54a9ea7eeb015f8`. It exercised the authentic public v0.1.8 NSIS app in an isolated synthetic fixture. It does not qualify a candidate installer, upgrade, signed updater or release.

All **30/30** hosted preflight contracts passed. Trusted WebView input reached Export. The Windows PowerShell 5.1 [native observation](evidence/native-save-observations.json) records the unique visible `Export profile` dialog in exact app PID 2148, with the `process-owned-top-level` route and the main `execs` / `Tauri Window`. Physical filename keystrokes and Tab left the `File name:` value exactly equal to the requested absolute ZIP path. The [native screenshot](evidence/03-native-export-save.png) shows that path and a visible Save button; UI Automation recorded the Save control as enabled with automation id `1`.

The helper then called UI Automation `SetFocus()` on Save, but the control's `HasKeyboardFocus` remained false. It **stopped before Enter**, so this run does not test whether physical Save activation would return a selection or produce a ZIP. The [result](evidence/result.json) retained no exported file or normal close; the final synthetic fixture bytes were preserved by forced cleanup. The [failure WebView capture](evidence/failure-last-webview.png) still shows the app waiting for the open native operation. This is a harness focus boundary, not evidence of a product export defect.

Microsoft documents that [UI Automation focus behavior depends on the provider](https://learn.microsoft.com/en-us/dotnet/api/system.windows.automation.automationelement.setfocus?view=windowsdesktop-10.0) and that [Enter activates a dialog's default button](https://learn.microsoft.com/en-us/windows/win32/uxguide/inter-keyboard). The bounded follow-up refocuses the exact filename edit, which already demonstrated keyboard focus, and sends Enter only if [DM_GETDEFID](https://learn.microsoft.com/en-us/windows/win32/dlgbox/dm-getdefid) reports Save control id `1` as the default button. It rechecks the exact app process, unique foreground dialog, enabled visible Save control, filename focus/value, destination containment and no existing target immediately before input. The read-only default-button query has a one-second timeout and never changes the dialog default. Exact ZIP verification remains mandatory; alternate paths remain diagnostics only. All **31/31** focused local Windows contracts pass in PowerShell 5.1 and 7, including refusal of wrong filenames, missing focus, disabled/hidden Save and non-Save defaults. The native script parses and its Win32 interop compiles in both editions; Biome and whitespace checks pass. A new hosted run is required to exercise this route and verify export and normal close.

The original workflow artifact is API artifact `10788892751`, named `windows-previous-public-capability-04e83db6a3ec0e28bc27b9ada54a9ea7eeb015f8`. Its **52 original files** are retained byte-for-byte in [evidence](evidence), with independently rechecked sizes and SHA-256 hashes in [provenance.json](provenance.json). Public-repo review of the four screenshots and retained text found synthetic fixture content and ephemeral GitHub runner identity/paths only. Credential-pattern hits were limited to .NET's public assembly `PublicKeyToken`; no credentials were found.
