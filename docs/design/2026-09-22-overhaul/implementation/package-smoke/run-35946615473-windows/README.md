# Windows previous-public capability: run 35946615473

This [hosted run](https://github.com/rndaom/execs/actions/runs/35946615473) **failed** on PR head `dbaae5ac74b0476f689ff9d38262f45f94f7871d` through tested merge `5e525e636d1b2bfe660738ccea5e30d4d66ca03e`. It probes the actual public v0.1.8 NSIS installer. It does not qualify a candidate Windows installer, upgrade, or signed updater.

The harness verified the public installer's signature and exact release hash, installed it into the disposable root, and observed the production app and WebView2 process. The private WebView2 debug listener and EdgeDriver listener were bound to loopback. A trusted native click opened the profile menu, and a native Tab focused New profile. The retained [result](evidence/result.json) then failed while classifying a native click on the active profile's Export control: the control's final bounding box differed from its box before the click. The harness discarded that second click's event trace before saving the result, so this run cannot prove where its pointerdown and click landed.

The [menu screenshot](evidence/02-old-profile-menu.png) precedes that click. The [failure screenshot](evidence/failure-last-webview.png) shows the menu shifted downward beneath a “Wait for the current operation to finish” banner, while Comfig displays “Working…”. In public v0.1.8, Export sets the shared busy state, and Comfig renders “Working…” whenever that state is set. These observations are consistent with Export taking effect and moving the layout after the click, but the missing second-click trace leaves that conclusion unverified. No native Save result or export ZIP was captured. The separate UI Automation assembly load error remains in the inspection receipt; WebDriver supplied the menu evidence.

The run's owned-process cleanup completed, registry policy restoration reported success, and the final synthetic fixture comparison reported its original bytes preserved. The probe stopped before export verification, candidate installation, import, or reopen. It is **not package qualification**.

The original workflow artifact is API artifact `10786587916`, named `windows-previous-public-capability-5e525e636d1b2bfe660738ccea5e30d4d66ca03e`. Its 48 extracted files are archived byte-for-byte beneath this directory, including the screenshots and synthetic fixture files. [provenance.json](provenance.json) records their sizes and SHA-256 hashes. A follow-up harness change records each native pointer event's target box, classifies the trusted pointerdown and click against those event-time boxes, and saves the trace before classification. That change requires a new hosted run to establish whether the Export click and subsequent native Save dialog succeed.
