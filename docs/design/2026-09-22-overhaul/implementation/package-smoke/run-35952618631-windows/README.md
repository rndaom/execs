# Windows previous-public capability: run 35952618631

The [hosted run](https://github.com/rndaom/execs/actions/runs/35952618631) **failed** on PR head `aa96fe8308dd16b3dbb5c272bbda762b295774f1` through tested merge `010938deb43fec9129908efe96324c9adc5b2bf7`. It exercised the authentic public v0.1.8 NSIS app with a synthetic fixture. It does not qualify a candidate installer, upgrade, signed updater or release.

All **31/31** hosted preflight contracts passed. Trusted WebView input reached Export. The Windows PowerShell 5.1 [native observation](evidence/native-save-observations.json) records the unique foreground, visible `Export profile` dialog in exact app PID 7984. Its owner is zero; the narrow `process-owned-top-level` route is supported by one visible `execs` / `Tauri Window` main window in that same process. Physical filename keystrokes and Tab left the `File name:` field exactly equal to the requested absolute ZIP path. The Save control was visible and enabled with automation id `1`. The [native screenshot](evidence/03-native-export-save.png) shows the focus outline on the `Save as type: Zip` combo box after Tab.

The helper then called UI Automation `SetFocus()` on the filename edit, but `HasKeyboardFocus` stayed false. It **stopped before querying the default button or sending Enter**. The retained observation did not record the actual focused UI Automation element or `DM_GETDEFID` value, so the screenshot's focus indication is visual evidence, not a completed keyboard-focus measurement. The [result](evidence/result.json) contains no exact ZIP export or normal close; the final synthetic fixture bytes were preserved by forced cleanup. The [failure WebView capture](evidence/failure-last-webview.png) shows the app waiting for the still-open native operation. This run establishes a harness focus failure and does not establish a product export defect.

The bounded follow-up identifies the exact `Save as type:` combo by UI Automation type, name and id, checks [the actually focused element](https://learn.microsoft.com/en-us/dotnet/api/system.windows.automation.automationelement.focusedelement?view=windowsdesktop-10.0) against that combo, and sends one physical [Shift+Tab](https://learn.microsoft.com/en-us/dotnet/api/system.windows.forms.sendkeys?view=windowsdesktop-10.0) to return to the filename edit. It polls for the filename's actual focus, then repeats exact process, unique foreground dialog, field value, path containment, visible enabled Save, and no-overwrite checks. [DM_GETDEFID](https://learn.microsoft.com/en-us/windows/win32/dlgbox/dm-getdefid) must report Save id `1` as the dialog default before physical Enter. Focus observations are bounded to the owned dialog, and any unexpected focus or dialog state stops the helper. Exact requested ZIP verification remains the only success condition; alternate paths are diagnostic only. All **31/31** focused local Windows contracts pass, including the Framework UI Automation focus ancestry check in PowerShell 5.1 and the prior wrong-default, lost-focus and ownership refusal cases. The native scripts parse, Biome passes, and whitespace checks pass. A new hosted run is required to exercise this route and verify export and normal close.

The original workflow artifact is API artifact `10789346581`, named `windows-previous-public-capability-010938deb43fec9129908efe96324c9adc5b2bf7`. Its **52 original files** are retained byte-for-byte in [evidence](evidence), with independently rechecked sizes and SHA-256 hashes in [provenance.json](provenance.json). Public-repo review of the four screenshots and retained text found synthetic fixture content and ephemeral GitHub runner identity/paths. The text scan found no email, credential assignment, bearer token, GitHub/AWS token or private-key pattern; no credentials were identified.
