# Windows previous-public capability: run 35949580124

The [hosted run](https://github.com/rndaom/execs/actions/runs/35949580124) **failed** on PR head `3843e12149409193c02567b64d107614eac782c2` through tested merge `0d722bd2f7aa10c57ba492cf78a9787eecad39b3`. It probes the authentic public v0.1.8 NSIS installer. It does not qualify a candidate Windows installer, upgrade, or signed updater.

All **28/28** hosted preflight contracts passed. The previous-public app launched in its isolated fixture, the WebView2 listener stayed on loopback, and trusted native input reached Export. The Windows PowerShell 5.1 [Save receipt](evidence/08-save-receipt.json) confirms the exact app-process dialog, the `process-owned-top-level` identity route, the requested absolute ZIP path accepted by the `File name:` control, and a returned Save `InvokePattern` call. The [pre-invoke capture](evidence/03-native-export-save.png) visibly shows that path and the native Save button.

The [result](evidence/result.json) then timed out waiting for the **exact** requested ZIP. Its final fixture inventory lists no file in the exports directory. No ZIP was archived or inspected, and no normal-close request occurred. The existing artifact has no post-invoke native dialog snapshot: the regular `Inspect` action still uses PowerShell 7, whose UI Automation load fails, while the retained [failure WebView capture](evidence/failure-last-webview.png) cannot show whether the native dialog stayed open or displayed an error. The evidence therefore does not distinguish a dismissed dialog with a product export failure, an unresolved native dialog, or a file written elsewhere. The empty previous-app log adds no diagnosis.

The follow-up adds a **read-only diagnostic** when that exact-path wait fails. A hosted-gated Windows PowerShell 5.1 `Observe` action rechecks the pinned app process and records its native windows, bounded controls of visible native dialogs, and a screenshot only when the foreground window has both UI Automation and Win32 ownership by that process. It checks four explicit file names: the exact destination, an adjacent appended `.zip`, and the basename and appended `.zip` in the disposable runner's Documents directory. Alternate paths are observations only; they never satisfy export verification. The existing WebView state is also retained before cleanup. `Observe` can write only its capture within the owned evidence directory, while Save's capture and observation outputs now have the same boundary. A focused fixture verifies those four paths and ignores an unrelated Documents file. All **29/29** local Windows contracts, PowerShell 5.1 parsing, Biome, and whitespace checks pass. A new hosted run is required to classify the post-invoke state and complete export verification.

The failed probe force-cleaned nine tracked child processes, restored all four policy entries, and reported original synthetic fixture bytes preserved. The original workflow artifact is API artifact `10788715558`, named `windows-previous-public-capability-0d722bd2f7aa10c57ba492cf78a9787eecad39b3`. Its 52 extracted files are archived byte-for-byte under [evidence](evidence), with independently verified sizes and SHA-256 hashes in [provenance.json](provenance.json). The public-repo review found synthetic fixture content and ephemeral GitHub runner paths/identity only, with no credentials in retained text or screenshots.
