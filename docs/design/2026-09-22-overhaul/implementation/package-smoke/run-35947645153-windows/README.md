# Windows previous-public capability: run 35947645153

This [hosted run](https://github.com/rndaom/execs/actions/runs/35947645153) **failed** on PR head `fac08e0ff7b01f4a7da39655ed7e6efc17c84978` through tested merge `9b07249bd1dae15290d6bd9812b532b1bc9fa557`. It probes the actual public v0.1.8 NSIS installer. It does not qualify a candidate Windows installer, upgrade, or signed updater.

The harness verified the public installer's signature and exact release hash, installed it into the disposable root, and observed the production app with WebView2 and loopback-only debug listeners. A trusted native click opened the profile menu, and a native Tab focused New profile. The retained [Export click trace](evidence/result.json) then records trusted pointerdown and click events at `(487, 160)`, both on the intended Export control. Its event-time box stayed at left `473.75`, top `146.046875`, right `501.75`, bottom `174.046875`; the final top moved to `153.765625` after the click. The revised classifier correctly accepted that post-click movement. This proves native input reached Export, not that export completed.

The run invoked the native Save helper through `pwsh`, then failed with `Could not load file or assembly 'UIAutomationTypes, Version=10.0.0.0'` in [11-save-error.txt](evidence/11-save-error.txt). The preceding [inspection receipt](evidence/10-inspect-receipt.json) has the same UI Automation error and no window observation. No native Save result, dialog capture, or export ZIP was retained. The exact failure arises in the harness's UI Automation path; the product's export result remains untested. [Microsoft documents](https://learn.microsoft.com/en-us/dotnet/framework/ui-automation/ui-automation-overview) UIAutomationClient and UIAutomationTypes as managed UI Automation client assemblies, and [PowerShell editions](https://learn.microsoft.com/en-us/powershell/module/microsoft.powershell.core/about/about_powershell_editions) explains that Windows PowerShell 5.1 uses .NET Framework while PowerShell 7 uses the Core runtime.

The follow-up harness fix runs only native Save and Close through the system Windows PowerShell 5.1 in STA, retaining the other actions on `pwsh`. Local compatibility tests load the installed Framework UI Automation assemblies and obtain `RootElement` in 5.1; local `pwsh` did not obtain it. The fix also keeps exact owned-root, process-identity, and reparse-point checks, with a Framework-compatible fully qualified path predicate. [Microsoft notes](https://learn.microsoft.com/en-us/dotnet/api/system.io.path.ispathfullyqualified) that a rooted path can still depend on the current directory; the predicate rejects drive-relative and root-relative paths. The 5.1 session uses `RemoteSigned` only for that process to run the checked-out helper; [Microsoft documents](https://learn.microsoft.com/en-us/powershell/module/microsoft.powershell.core/about/about_execution_policies?view=powershell-5.1) that this session setting does not change the registry and does not override Group Policy. A new hosted run is required to verify the full Save and Close path.

Owned-process cleanup completed, registry policy restoration reported success, and the final synthetic fixture comparison reported its original bytes preserved. The probe stopped before export verification, candidate installation, import, or reopen. It is **not package qualification**.

The original workflow artifact is API artifact `10786914941`, named `windows-previous-public-capability-9b07249bd1dae15290d6bd9812b532b1bc9fa557`. Its 56 extracted files are archived byte-for-byte beneath this directory, including screenshots, trace, error receipt, and synthetic fixture files. [provenance.json](provenance.json) records their sizes and SHA-256 hashes.
