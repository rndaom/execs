# Linux development packages: run 35780702038

This [hosted run](https://github.com/rndaom/execs/actions/runs/35780702038) tested PR head `ee0a8f22db5f6a07af053bd070490d55c9f271e9` through merge `3b979c6cdbcf0406e8c5cdc691150f2917cc70b3`. The workflow **failed overall** in its Debian case. The AppImage case passed its complete, separate sequence; neither Debian case reached installation or runtime. This is development qualification, not a signed updater or release result.

The optimized AppImage and Debian builds and the 470-package notices check passed. Authentic published v0.1.8 AppImage and Debian assets passed their signature and hash checks before native work. The previous AppImage launched from the private read-only FUSE mount. Native clipboard input read the exact 55-byte config. Its real GTK Save dialog accepted the specified export path, and an independent bounded ZIP inspection verified all four payload hashes and the manifest. The export is retained as [`previous-ui-export.zip`](evidence/appimage-upgrade/previous-ui-export.zip), SHA-256 `6f93d339d2c9cfdafaad7f3c754fbbe74b87a1023cd832a70802b429bc261bd2`.

After a complete AppImage replacement, the candidate opened the original profile, imported that ZIP through the native chooser and review, switched to the imported profile, and reopened with the selected config intact. Three native close requests were followed by owned-process disappearance before cleanup; the external driver does not expose the app's exit code. Exact fixture comparisons passed at fourteen checkpoints, including final cleanup, across the original and imported libraries, synthetic live files and settings. The real updater UI and signed self-update were not exercised. The candidate AppImage SHA-256 was `ac8797673d449c97c52ea35e555c88fd8da8ee1227abd314f66f13137b45a927`, and its inspected packaged ELF SHA-256 was `2fc77ba4627e740ee22ac12754eb0adf027dda83eec874412e56fb7993213778`.

Root inspected all twelve archived PNGs individually: previous Files read, GTK Export before and after exact path entry, Export completion, candidate Files read, native Import before and after path entry, review, import completion, switch completion, selected Files read and reopen. All are nonblank, legible and correspond to the named steps. The screenshots support the visible flow; the structured [case result](evidence/appimage-upgrade/results.json), retained ZIP and preserved fixture hashes support the byte claims.

The Debian case stopped at a harness assertion before any Debian install. It compared the inspected `.deb` ELF SHA-256 `3d6d40fb743b03e46af9909e5ed861081634aa738a1a2c4cf04f5bf0f5cdea58` to the unpatched build ELF SHA-256 `e0eff8183241835bb3d7bab989730b91d2c0b37add4ab651e57be5968bb11791`. That equality assumption is invalid: [Tauri's bundler source](https://github.com/tauri-apps/tauri/blob/dev/crates/tauri-bundler/src/bundle.rs) patches a bundle-kind marker for each package and then restores the build ELF. A subsequent harness correction requires the Debian ELF to differ from the build ELF **only at that exact marker**, while the installed and running executable must still hash to the inspected package ELF. The correction has data-only tests; it has not converted this failed run into a Debian pass. Debian upgrade, first installation and their UI round trips remain unverified.

The original downloaded workflow artifact is API artifact `10718307664`, named `development-linux-packages-3b979c6cdbcf0406e8c5cdc691150f2917cc70b3`. Its 21 extracted files are archived byte-for-byte under [`evidence/`](evidence/) with their hashes in [`provenance.json`](provenance.json). The root [`results.json`](evidence/results.json) records the overall failure; the AppImage and Debian child results retain their separate statuses. Local log files in the evidence are original output and may contain trailing whitespace.
