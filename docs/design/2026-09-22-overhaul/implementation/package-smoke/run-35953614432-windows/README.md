# Windows previous-public capability: run 35953614432

The [hosted run](https://github.com/rndaom/execs/actions/runs/35953614432) **passed** on PR head `b13d24ec692b4af72ffc23187fcf19d08c1264fa` through tested merge `fc9cc441a8853ac3d080871a72bcdf25cc93e63e`. It exercised the authentic public v0.1.8 NSIS app in an isolated synthetic Windows fixture. All **31/31** hosted preflight contracts passed. The [result](evidence/result.json) records a verified public release asset, valid installer signature, successful install into the fixture, production WebView2 attachment on an owned loopback listener, and trusted profile-menu and Export clicks.

The [native Save receipt](evidence/08-save-receipt.json) identifies one visible foreground `Export profile` dialog in exact app PID 8144, with the bounded `process-owned-top-level` route. Physical keystrokes entered the requested absolute path, Tab focused `Save as type: Zip`, and Shift+Tab returned actual UI Automation focus to the `File name:` edit. The field value and requested destination were identical. Windows `DM_GETDEFID` returned `0x534B0001`: Save control id `1` was the dialog default. After the guarded physical Enter, the exact [export ZIP](evidence/previous-ui-export.zip) appeared and the [export-complete capture](evidence/04-export-complete.png) shows the old app back on its main view.

I independently rehashed the retained ZIP: **1,674 bytes**, SHA-256 `6f93d339d2c9cfdafaad7f3c754fbbe74b87a1023cd832a70802b429bc261bd2`. Its five member names, sizes and hashes exactly match the result's archive inventory; all four payload members match the retained original profile files or shared blob byte-for-byte. The manifest names the active synthetic profile and lists the expected four TF2 payload paths. The runner's app-data and live TF2 hash inventories match before install, after install and after normal close; the final export inventory contains only the exact requested ZIP. The retained original fixture files independently match the runner's initial inventory. The runner requested an owned native window close and observed app exit code `0` **before cleanup**, with `forced: false`; cleanup then stopped only tracked helper processes and restored policy. The final fixture state is recorded as hashes rather than retained final file bytes, so that preservation conclusion is bounded by the runner's inventories and checks.

This pass proves the previous-public Windows install, production WebView2 input, native export and normal-close capability in the disposable fixture. It does **not** exercise a candidate 0.2.0 NSIS installer, previous-public-to-candidate upgrade/import/reopen, signed updater, standard-user Windows, retail TF2, Steam Cloud, screen-reader speech, or release publication. Those gates remain open.

The original workflow artifact is API artifact `10789049848`, named `windows-previous-public-capability-fc9cc441a8853ac3d080871a72bcdf25cc93e63e`. Its **55 original files** are retained byte-for-byte in [evidence](evidence); [provenance.json](provenance.json) records each file's size and SHA-256. Every entry was independently rechecked against the downloaded source and archive copy, with no missing, extra or mismatched files. The four screenshots show synthetic fixture content. A retained-text scan found no email, credential assignment, bearer token, GitHub/AWS token or private-key pattern; logs and paths contain only ephemeral GitHub runner identity and fixture locations. The artifact API reports an archive digest, recorded as API metadata in provenance; the byte-level checks above apply to the extracted original files.
