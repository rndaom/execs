# Windows previous-public capability: run 35950498726

The [hosted run](https://github.com/rndaom/execs/actions/runs/35950498726) **failed** on PR head `7dfe0e7f25d1d1c1cf1d95a373fe5a256ca5e841` through tested merge `5188f5d450bd79d806a331c5dcbd766ddc376090`. It exercised the authentic public v0.1.8 NSIS app in an isolated synthetic fixture. It does not qualify a candidate Windows installer, upgrade, signed updater, or release.

All **29/29** hosted preflight contracts passed. Trusted WebView input reached Export. The Windows PowerShell 5.1 [Save receipt](evidence/08-save-receipt.json) records the unique, foreground `Export profile` dialog in the exact installed app process, using the `process-owned-top-level` identity route. The `File name:` value matched the requested absolute ZIP path, and Save `InvokePattern` returned. The [pre-invoke screenshot](evidence/03-native-export-save.png) shows that value and the Save button.

The exact-path wait timed out with no ZIP in the fixture exports directory. The [post-invoke native observation](evidence/09-observe-receipt.json) found no `Export profile` dialog, no visible native dialog controls and no observation error. The installed app process was still alive; its `execs` / `Tauri Window` main window was foreground. The [owned-screen capture](evidence/04-native-after-save.png) and retained [WebView capture](evidence/failure-last-webview.png) show the idle profile menu without an export error. Four bounded candidate names were absent: the exact destination, an adjacent `.zip.zip`, and the equivalent two names in the runner's Documents directory. The original suggested `Package smoke - active.zip` in Documents was **not checked in this run**. The result recorded no exported file, ZIP inspection or normal close. The dialog's disappearance and lack of frontend error do not establish whether the old app returned a cancelled selection or wrote elsewhere; no product defect or successful export is established.

The follow-up harness uses physical [SendKeys input](https://learn.microsoft.com/en-us/dotnet/api/system.windows.forms.sendkeys?view=windowsdesktop-10.0) for the exact fixture filename and focused Save button, in place of UI Automation's [programmatic value](https://learn.microsoft.com/en-us/dotnet/api/system.windows.automation.valuepattern.setvalue?view=windowsdesktop-10.0) and [invoke](https://learn.microsoft.com/en-us/dotnet/api/system.windows.automation.invokepattern.invoke?view=netframework-4.8.1) operations. It refuses paths with SendKeys metacharacters, proves the exact field value after typing and focus change, and rechecks the pinned process, unique foreground dialog, focused Save control, destination containment and absence immediately before Enter. On an exact-path timeout only, the hosted-gated, read-only observation also checks the single suggested filename in Documents. Alternate paths remain diagnostic and cannot satisfy success. The 30 focused local Windows contracts pass in PowerShell 5.1 and PowerShell 7; Biome and whitespace checks pass. The physical-input route and any export/close result need a new hosted run.

The original workflow artifact is API artifact `10788247120`, named `windows-previous-public-capability-5188f5d450bd79d806a331c5dcbd766ddc376090`. Its **55 original files** are archived byte-for-byte in [evidence](evidence), with independently rechecked sizes and SHA-256 hashes in [provenance.json](provenance.json). Public-repo review of all five screenshots and retained text found synthetic fixture content and ephemeral GitHub runner identity/paths only. Credential-pattern hits were limited to .NET's public assembly `PublicKeyToken`; no credentials were found.
