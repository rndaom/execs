# Native Windows verification — 22 September 2026

**Scoped functional pass:** one Windows GUI build completed startup-error, rootless App settings, import-review cancellation and inactive-profile deletion checks (captures 01–18). A subsequent build verified the corrected inactive-library state, chooser keyboard behavior and import wording (captures 19–23). A final build received a two-profile chooser geometry check (capture 24). The build identities and limits remain separate below. This is local candidate evidence, not installer or release qualification.

**Build for captures 01–18:** `pnpm desktop build --no-bundle`, version **0.2.0**, 23,439,872 bytes, SHA-256 **`E7BC62505EFC1257ACE629EDECB8640EDAEE3860CA2505DED68038FF00D40FAD`**. Its PE subsystem is Windows GUI (2). Native interaction and captures were performed by the root agent with the official Windows computer-use tool; supporting files were collected separately without further UI interaction.

| Completed check | Result and retained evidence |
| --- | --- |
| Startup failure before WebView2 | An invalid private maintenance marker produced the native error dialog without a console. Version, actionable error and state location were available through UIA. The dialog visually abbreviates long paths; **Ctrl+C retained the complete paths and message**. OK exited with code 0. Marker SHA-256 remained `0D6CEAC6342CD664CD9816C782F27C9A121B145868292BCB0B1DF59CAC9325B5`. [Capture](01-startup-error.jpg), [UIA text](01-startup-error-accessibility.txt), [copied message](01-startup-error-copied.txt), [exit evidence](metadata/20260922-120326-startuperror-7da431d8/exit.json). |
| App settings before setup | App settings opened from the rootless finder; no TF2 folder was confirmed and no profile existed. [Finder](02-rootless-finder.jpg), [settings before changes](03-app-settings-before.jpg). |
| Native preferences and keyboard | Selecting the visible Reduce label saved `motion: "reduce"`. Physical Tab exposed the update switch's visible focus ring; Space enabled startup checking and saved `checkForUpdatesOnStartup: true`. [Reduce selected](04-reduced-motion-saved.jpg), [keyboard focus](05-update-switch-keyboard-focus.jpg), [saved state](06-app-settings-saved.jpg), [first-session settings bytes](metadata/20260922-120533-rootless-422d593e/first-session/settings.json). |
| Copy data location | The operator verified the clipboard exactly matched `G:\Projects\execs\.artifacts\native-isolation\20260922-120533-rootless-422d593e\roaming\execs`. This is an operator observation; no separate clipboard artifact was retained for this action. The displayed location is present in [UIA evidence](06-app-settings-saved-accessibility.txt). |
| Install picker cancellation | Browse opened the native folder picker. Escape only left its address-entry mode; the visible Cancel button dismissed the picker. The finder returned and persisted `tf2Root` remained the empty string. The discovered existing install was never confirmed. [After cancellation](07-picker-canceled.jpg), [settings bytes](metadata/20260922-120533-rootless-422d593e/first-session/settings.json). |
| Close and reopen | Normal Alt+F4 closed the first rootless session with code 0. Reopening the same isolated case retained both preferences; the second normal close also returned 0. [Reopened view](08-preferences-after-reopen.jpg), [first exit](metadata/20260922-120533-rootless-422d593e/first-session/exit.json), [reopened settings](metadata/20260922-120533-rootless-422d593e/reopened-session/settings.json), [second exit](metadata/20260922-120533-rootless-422d593e/reopened-session/exit.json). |
| Manual native update check | The operator observed Checking, then “You're on the latest version.” The local development version was 0.2.0 while the public release was 0.1.8. No update was downloaded or installed. This verifies the check/result path, not an upgrade or stale-offer scenario. [Settled result](09-native-update-check.jpg), [UIA result](09-native-update-check-accessibility.txt). |
| Import review and cancel | From two inactive synthetic profiles, Import opened the native picker and reviewed the retained v0.1.8 ZIP. The operator selected `secondhud`, expanded Import details, and canceled. The fixture assertion then passed with the exact original file inventory/bytes and null active id. Import was never confirmed. [Library/menu](11-inactive-profile-menu.jpg), [review](12-native-import-review.jpg), [selection](13-native-import-hud-selected.jpg), [details](14-native-import-details.jpg), [canceled](15-native-import-cancelled.jpg). |
| Delete one inactive profile | Actions → Delete profile opened the inactive-only confirmation; confirming removed **QA inactive - no HUD**. The app exited normally at 16:26:46 UTC. Independent read-only validation found exactly its three exclusive files and manifest removed, only the index changed, and all 15 other files identical. Active id stayed null; the single-HUD profile remained. No staging, journal, part, maintenance or preloader remnants remained. [Actions](16-native-profile-actions.jpg), [confirmation](17-native-inactive-delete-confirmation.jpg), [completed](18-native-inactive-delete-completed.jpg), [structured integrity result](metadata/20260922-120750-inactivelibrary-ff33a080/post-delete-integrity.json). |

All successful launches came from the existing **Explorer desktop**, through the bundled **PowerShell 7.6.5** host using its existing **RemoteSigned** policy. No execution-policy override or registry change was made. `GetPackageFullName` returned **15700** for Explorer, the helper and execs in each successful session. [Startup launch](metadata/20260922-120326-startuperror-7da431d8/launch.json), [rootless first launch](metadata/20260922-120533-rootless-422d593e/first-session/launch.json), [reopened launch](metadata/20260922-120533-rootless-422d593e/reopened-session/launch.json), [first inactive-library launch](metadata/20260922-120750-inactivelibrary-ff33a080/launch.json), [targeted recheck launch](metadata/20260922-123141-inactivelibrary-e497a50a/launch.json), [host policy](metadata/20260922-123141-inactivelibrary-e497a50a/bootstrap.json).

**WebView2 153.0.4234.48** used the fixture's `webview2\EBWebView` directory, confirmed by the actual browser process command lines. APPDATA, LOCALAPPDATA, XDG paths and temporary files were also redirected to the case. Tauri's separate default KnownFolder cache directory already existed; its existence is recorded in the launch logs. No WebView2 cache, real profile/config files or Steam account files were copied into this evidence folder. [WebView2 evidence](metadata/20260922-120533-rootless-422d593e/reopened-session/webview-processes.json).

The first attempt, `20260922-115611-startuperror-6775da61`, did **not** launch execs: Windows PowerShell 5.1 refused the local script under that host's normal policy. The exact [event 4100](metadata/20260922-115611-startuperror-6775da61/bootstrap-policy-refusal-event.json) and [unexecuted request](metadata/20260922-115611-startuperror-6775da61/request.json) are retained. That request references the earlier `B8CCBBEA…` binary, which has no successful app evidence here. Switching to the existing bundled host required no policy change.

**Capture and accessibility limits:** captures 02–24 contain the complete 1200 × 800 app client plus the OS window frame; capture 01 is the native error dialog. Each has a matching `-accessibility.txt` file. The target window was explicitly activated for retained captures. An initially occluded frame and snapshots taken before UI state settled were not archived. The folder-picker screenshot showing unrelated Downloads contents was not retained. UIA focus metadata was sometimes stale, and directly invoking a visually hidden radio input did not select it; clicking the visible label and using physical keyboard input worked. These checks do not establish screen-reader speech behavior.

**Reproduction:** [Start-IsolatedExecs.ps1](reproduction/Start-IsolatedExecs.ps1) and [InactiveLibraryFixture.ps1](reproduction/InactiveLibraryFixture.ps1) archive the current local QA scripts. [Validate-InactiveDeletion.ps1](reproduction/Validate-InactiveDeletion.ps1) and [Validate-InactiveReview.ps1](reproduction/Validate-InactiveReview.ps1) compare the executed fixtures against their original baselines without changing fixture files. Text artifacts use UTF-8/LF and repository formatting; provenance records separate source and archived hashes. Hashes inside integrity results describe the executed source files, before archival text normalization. Image bytes remain unchanged. Absolute paths deliberately describe this executed local workspace. New runs create fresh directories beneath `G:\Projects\execs\.artifacts\native-isolation`, bind themselves to the current executable hash, and refuse another running execs process. Preserve historical cases instead of reusing them after rebuilding. The launcher gained its inactive-library preparation branch after the first rootless session; the archive records the reusable script snapshot at collection, not an unrecorded historical script hash. [Copied-file provenance](metadata/archive-copies.json), [captured-text normalization](metadata/captured-text-normalization.json), [unchanged capture hashes 01–24](metadata/capture-hashes-01-24.json). Earlier [01–09](metadata/capture-hashes-01-09.json), [01–18](metadata/capture-hashes-01-18.json) and [01–23](metadata/capture-hashes-01-23.json) hash sets remain retained.

```powershell
& 'G:/Projects/execs/.artifacts/native-isolation/Start-IsolatedExecs.ps1' -Scenario StartupError
& 'G:/Projects/execs/.artifacts/native-isolation/Start-IsolatedExecs.ps1' -Scenario Rootless
# After closing that Rootless session normally, reuse its private preferences:
& 'G:/Projects/execs/.artifacts/native-isolation/Start-IsolatedExecs.ps1' -Resume -CaseDirectory '<new rootless case>'
```

The second command must wait until the first app has closed. Startup update checks begin disabled in a fresh fixture. Rootless scope permits preferences, copying and picker cancellation only; no install confirmation, profile mutation, TF2 launch or update installation was performed.

**Inactive-library integrity:** case `20260922-120750-inactivelibrary-ff33a080` used the same `E7BC6250…` executable. Its baseline contained 20 files; the post-delete state contains 16. Eight remaining-profile files, one shared blob, four synthetic-live files, the import ZIP and settings are unchanged. Removed id: `e8be0ee4-6480-4836-8013-01401d858c59`; retained id: `7c6441ad-2d6d-4e79-a451-ad3e3ad27fcd`. [Baseline](metadata/20260922-120750-inactivelibrary-ff33a080/inactive-library-baseline.json), [remaining index](metadata/20260922-120750-inactivelibrary-ff33a080/index-after-delete.json), [normal exit](metadata/20260922-120750-inactivelibrary-ff33a080/exit.json), [independent comparison](metadata/20260922-120750-inactivelibrary-ff33a080/post-delete-integrity.json), [prepared-scope proof](reproduction/inactive-library-safety.md). The production app can still discover actual Steam/Cloud paths independently of redirected app data; activation and general live-setting tests remain outside this fixture's scope.

**Findings retained in the historical native captures:** the no-active-profile body was blank ([10](10-inactive-library-before.jpg)), and import review said “Your current profile stays active until you switch” despite there being no active profile ([12](12-native-import-review.jpg)). Profile management fixed both in source after the `E7BC6250…` binary was built. Captures 10–18 retain that earlier build; the separate targeted recheck below verifies the corrections.

**Build for captures 19–23:** a subsequent `pnpm desktop build --no-bundle` completed in 3 minutes 9 seconds. The executable remained version **0.2.0**, 23,439,872 bytes, with SHA-256 **`F6A0B0DE2B33E51A2F65D8BBFA25CE5746E07A852A63F60FE4AEC00AA538409F`**. Fresh case `20260922-123141-inactivelibrary-e497a50a` used app PID 16372, helper PID 9296 and Explorer PID 30156, each returning package code 15700. The app exited normally with code 0 at 16:35:00 UTC. [Exact request](metadata/20260922-123141-inactivelibrary-e497a50a/request.json), [launch](metadata/20260922-123141-inactivelibrary-e497a50a/launch.json), [exit](metadata/20260922-123141-inactivelibrary-e497a50a/exit.json), [isolated WebView2 processes](metadata/20260922-123141-inactivelibrary-e497a50a/webview-processes.json).

| Targeted recheck on F6 | Result and retained evidence |
| --- | --- |
| Inactive-library state | “Choose a profile” and its explicit switch warning replaced the blank body. Choose profile opened the header chooser. [Corrected state](19-native-inactive-library-corrected.jpg), [chooser opened](20-native-chooser-opened.jpg). |
| Physical keyboard navigation | The operator used Tab to reach the first Actions control and Shift+Tab to return to the first profile row; its visible focus ring was captured without activating a profile. Escape closed the chooser and returned visible focus to Profiles. Return reopened it. [Profile-row focus](21-native-chooser-keyboard-focus.jpg), [returned summary focus](22-native-chooser-escape.jpg). |
| Corrected import review and cancel | Reviewing the fresh case's copied v0.1.8 ZIP displayed “Your TF2 setup stays unchanged until you switch.” The operator canceled; no import or profile activation was confirmed. [Corrected review](23-native-import-review-corrected.jpg), [UIA text](23-native-import-review-corrected-accessibility.txt). |
| Post-close integrity | The preparation assertion and an independently retained read-only report passed. All **20 files** retain their exact baseline bytes and inventory, both original profile ids remain, active id is null, and no recovery or staging remnants remain. [Baseline](metadata/20260922-123141-inactivelibrary-e497a50a/inactive-library-baseline.json), [post-cancel report](metadata/20260922-123141-inactivelibrary-e497a50a/post-cancel-integrity.json), [index](metadata/20260922-123141-inactivelibrary-e497a50a/index-after-cancel.json), [settings](metadata/20260922-123141-inactivelibrary-e497a50a/settings-after-cancel.json). |

The F6 pass did not repeat startup failure, rootless preferences, update checking or inactive deletion. Those outcomes remain tied to E7. The subsequent menu-position change has the separate native spot check below.

**Final geometry spot check, capture 24:** after the measured menu-position patch, `pnpm desktop build --no-bundle` completed in 3 minutes 8 seconds. Version **0.2.0**, 23,439,872 bytes, SHA-256 **`7D03B4A55499835DDEF908D228842347C60E8D2FD5803732B1DB8C0D8CBA6CA4`**. Product source was committed as `44bc5f4f54ba4d41bd9ddd27342223ae08b971da` while linking; the root agent recorded that source stayed unchanged during the build. Fresh case `20260922-124450-inactivelibrary-fc1d31c3` used helper PID 12084, app PID 11664 and Explorer PID 30156, all package code 15700. [Request](metadata/20260922-124450-inactivelibrary-fc1d31c3/request.json), [host policy](metadata/20260922-124450-inactivelibrary-fc1d31c3/bootstrap.json), [launch](metadata/20260922-124450-inactivelibrary-fc1d31c3/launch.json), [isolated WebView2](metadata/20260922-124450-inactivelibrary-fc1d31c3/webview-processes.json).

Choose profile opened the positioned two-profile menu with every action visible at 1200 × 800. Escape closed it and returned focus to Profiles; normal Alt+F4 exited with code 0 at 16:46:36 UTC. The reused read-only validator confirmed all **20 files** unchanged, both profile ids retained, active id null and no remnants. [Menu capture](24-native-final-positioned-menu.jpg), [UIA text](24-native-final-positioned-menu-accessibility.txt), [exit](metadata/20260922-124450-inactivelibrary-fc1d31c3/exit.json), [independent integrity report](metadata/20260922-124450-inactivelibrary-fc1d31c3/post-menu-integrity.json). No import or deletion was repeated on 7D; this check does not extend earlier builds' outcomes or establish native long-list/minimum-window behavior.

Still outside this pass: packaged NSIS installation/update/restart, Linux native behavior, dirty Files Save/Discard/Cancel on close, active-profile/live/Cloud writes, running-game transitions, screen-reader speech and the full native application matrix.
