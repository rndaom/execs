[workspace]

[package]
name = "execs-v018-compat-harness"
version = "0.0.0"
edition = "2021"

[dependencies]
old_core = { package = "execs-core", path = "../tagged-source/apps/desktop/src-tauri/core" }
new_core = { package = "execs-core", path = "G:/Projects/execs/apps/desktop/src-tauri/core" }
serde_json = "1"
zip = { version = "2", default-features = false, features = ["deflate"] }
