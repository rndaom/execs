# Program audit remediation

Updated as fixes are implemented and verified. A checked box means the change and its targeted test passed; it does not assert retail TF2, Steam Cloud, or installer qualification. Existing release gates remain in `docs/design/2026-09-22-overhaul/`.

**Status:** 34 of 35 findings are checked. D8's destination-bound native transport and Linux package dependency correction passed the scoped fixtures and hosted checks below. D7 remains open for documented third-party asset rights, cleared replacements or an owner-approved scope change. Release qualification remains separate.

## Integrity and trust

- [x] A1 — Kept external packs during switches
- [x] A2 — Active-profile deletion handoff
- [x] A3 — Viewmodel VPK member boundary
- [x] A4 — Managed Binds file preservation
- [x] A5 — Feature record provenance after drift
- [x] A6 — Imported feature metadata payload binding
- [x] A7 — Sound settings and file commit
- [x] A8 — Current setup cloning with live edits

## Effective state and conflicting content

- [x] B1 — External crosshair selection round-trip
- [x] B2 — Crosshair and Mod script collisions
- [x] B3 — HUD overlays and modded stock crosshair art
- [x] B4 — Later CFG and Launch override disclosure
- [x] B5 — Multiple keys for one binding
- [x] B6 — Global and per-class viewmodel visibility
- [x] B7 — Accepted external packs in Mods Installed
- [x] B8 — CFG failure scoped by dependent pane
- [x] B9 — Stale profile detail after failed load
- [x] B10 — Selected HUD projection in CFG inference
- [x] B11 — Competing installed hitsound sources

## Coverage and UI coherence

- [x] C1 — HUD Editor crosshair controls
- [x] C2 — Unsupported HUD Special operations
- [x] C3 — HUD schema compatibility coverage
- [x] C4 — Shared viewmodel preload ownership
- [x] C5 — Transparent viewmodel guidance
- [x] C6 — Partial custom crosshair builds
- [x] C7 — Comfig module Default labels
- [x] C8 — Mods Installed count and Casual changes

## External connections

- [x] D1 — Embedded mastercomfig guide window routing
- [x] D2 — HUD artwork revision pin
- [x] D3 — GameBanana GUI results and HUD routing
- [x] D4 — GameBanana download variant choice
- [x] D5 — GitHub HUD showcase cache expiry
- [x] D6 — Inventory probe documentation refresh
- [ ] D7 — Third-party permission follow-up
- [x] D8 — DNS precheck/connection binding review

## Verification log

Record the test, outcome, and any limit here when checking an item above.

- **Integrated gates:** `pnpm test` passed (963 desktop, 170 cfglint, 103 package-script tests with five expected skips) using the bundled Python runtime for package fixtures. Desktop TypeScript, `cargo fmt --check`, workspace Clippy with warnings denied, and `cargo test --workspace --locked` passed after the direct-DNS hardening (native 126/4 ignored; core 738/7 ignored; integration suites passed). `pnpm check` passes after narrowly excluding the archived qualification runs from Biome; their 117 retained artifact files were independently verified against sizes and SHA-256 values in three provenance manifests. Retail TF2, Steam Cloud, and packaged installer checks remain separate release qualification.
- **Hosted follow-up on product commit `65d20bd`:** [all five CI jobs](https://github.com/rndaom/execs/actions/runs/35937290554), the [native Linux development run](../../design/2026-09-22-overhaul/implementation/linux-native/run-35937290549/README.md), and the [unsigned Linux package run](../../design/2026-09-22-overhaul/implementation/package-smoke/run-35937290557/README.md) passed. The native active case now covers Cancel, Discard/restart and Save-and-close/restart; the package run covers AppImage upgrade, Debian upgrade and Debian first install. The [Windows previous-public capability run](../../design/2026-09-22-overhaul/implementation/package-smoke/run-35937290574-windows/README.md) failed before UI export/upgrade. These runs do not close D7/D8 or the remaining signed updater, Steam Cloud and retail TF2 gates.
- **Hosted follow-up on direct-DNS head `a077f16`:** [all five CI jobs](https://github.com/rndaom/execs/actions/runs/35940690985) and the [Linux native rerun](https://github.com/rndaom/execs/actions/runs/35940690981) passed. The [Linux package rerun](https://github.com/rndaom/execs/actions/runs/35940690993) failed at the candidate GTK Import chooser after previous-public export and candidate launch/read; the exact ZIP path was visible but the dialog stayed mapped after one Return. A guarded, import-only harness retry passed 44 focused local package tests and was used by both upgrade cases in [run 35942107825](../../design/2026-09-22-overhaul/implementation/package-smoke/run-35942107825/README.md), which passed AppImage upgrade, Debian upgrade and Debian first install. Its 45 artifact files are archived with SHA-256 provenance. The earlier Windows capability runs failed before UI export/upgrade; follow-up traced their later inspection/cleanup refusal to an ISO-string-versus-`DateTime` process creation comparison. [Windows run 35944325809](../../design/2026-09-22-overhaul/implementation/package-smoke/run-35944325809-windows/README.md) confirms the private app listener appears on `127.0.0.1` after startup, then fails because EdgeDriver binds `::`, its cleanup child is outside the old allowlist, and UI Automation does not load. All 51 artifact files are archived with SHA-256 provenance. The [next Windows run](https://github.com/rndaom/execs/actions/runs/35945890556) stopped in a new preflight console-host predicate fixture (23/24 tests passed) before any package probe. The hosted diagnostic identified the synthetic temp path alias RUNNER~1 versus its expanded runneradmin path; the literal observed D: path passed. The test now uses that canonical path, with production ownership checks unchanged. All 24 local tests passed; the next hosted run is recorded below. [CI](https://github.com/rndaom/execs/actions/runs/35944326001), [native Linux](https://github.com/rndaom/execs/actions/runs/35944325817) and [Linux packages](https://github.com/rndaom/execs/actions/runs/35944325738) passed on the UTC-tick fix head. These results do not close D7/D8 or the remaining release matrix.

- **Hosted follow-up on `dbaae5a`:** [All five CI jobs](https://github.com/rndaom/execs/actions/runs/35946615411), the [native Linux run](https://github.com/rndaom/execs/actions/runs/35946615400), and [unsigned Linux package run](https://github.com/rndaom/execs/actions/runs/35946615451) passed. The [Windows previous-public capability run](../../design/2026-09-22-overhaul/implementation/package-smoke/run-35946615473-windows/README.md) passed all 24 contract checks, installed and launched authentic v0.1.8, verified the private app and driver listeners on loopback, and opened the profile menu with trusted native input. It **failed before export verification** when the Export control moved during click classification; the old harness had not retained that click's event trace. Its 48 original artifact files are archived with verified SHA-256 provenance. A guarded event-time click classifier and trace persistence passed 64 focused contract tests with two Linux-only skips; the next hosted run is recorded below. No Windows package or candidate-upgrade pass is claimed.

- **Hosted follow-up on `fac08e0`:** The [native Linux run](https://github.com/rndaom/execs/actions/runs/35947645163) and [unsigned Linux package run](https://github.com/rndaom/execs/actions/runs/35947645148) passed. The [Windows previous-public capability run](../../design/2026-09-22-overhaul/implementation/package-smoke/run-35947645153-windows/README.md) recorded trusted pointerdown and click events on Export before its menu moved, then **failed before native Save or ZIP verification** because the PowerShell 7 helper could not load the .NET Framework UI Automation assembly. Its 56 original files are archived with verified SHA-256 provenance. Only Save and Close now use Windows PowerShell 5.1 in STA; the other harness actions still use PowerShell 7. Framework-compatible containment checks reject relative, drive-relative, root-relative, sibling and junction paths in both editions. All 27 local Windows capability contracts, Biome and whitespace checks pass; hosted native Save/Close remains unverified.

- **Hosted follow-up on `7e3ff44`:** The [Windows previous-public capability run 35948836291](../../design/2026-09-22-overhaul/implementation/package-smoke/run-35948836291-windows/README.md) passed 27 hosted preflight contracts, reached Export with trusted native input, and loaded UI Automation in the Windows PowerShell 5.1 Save helper. It **failed before filename entry, Save invocation, ZIP verification, or normal close** because the real, visible `Export profile` dialog had no Win32 owner (`GW_OWNER = 0`). Its process ID matched the exact installed app, and the same process had one visible `execs` / `Tauri Window` main window. All 51 original artifact files are archived with independently verified sizes and SHA-256 hashes. The harness now admits only that uniquely identified, foreground, process-owned top-level dialog or an explicit main-window owner; same-PID competing and unrelated dialogs, foreign processes, other owners, and changed foreground are rejected. Both PowerShell editions passed the new fixture, bringing focused local Windows contracts to 28/28; Biome and whitespace checks pass. A hosted rerun is required for actual Save, ZIP and close evidence. The 33/35 audit status and release gates are unchanged.

- **Hosted follow-up on `3843e12`:** The [Windows previous-public capability run 35949580124](../../design/2026-09-22-overhaul/implementation/package-smoke/run-35949580124-windows/README.md) passed 28 hosted preflight contracts. The native Save helper recorded the exact app dialog and filename, then returned from Save `InvokePattern`; the requested ZIP did **not** appear before the exact-path timeout. The final exports directory was empty, so there is no export payload or normal-close qualification. All 52 original artifact files are archived with verified sizes/SHA-256 hashes. The artifact lacks post-invoke native dialog state, so the cause remains undetermined. A hosted-only read-only observation now captures bounded owned-dialog controls, an owned foreground screenshot, WebView state, and presence of only four explicit exact/possible alternate names after timeout; alternate files never count as success. Capture paths are limited to the evidence directory. Local Windows contracts pass 29/29 in both PowerShell editions, with Biome, PS5 parsing and whitespace checks passing. The 33/35 audit status and release gates are unchanged.
- **Hosted follow-up on `7dfe0e7`:** The [Windows previous-public capability run 35950498726](../../design/2026-09-22-overhaul/implementation/package-smoke/run-35950498726-windows/README.md) passed 29 hosted preflight contracts and reached Save in the exact v0.1.8 app process. UI Automation accepted the requested filename and Save invocation returned, but the exact ZIP did **not** appear before timeout. New post-invoke evidence shows the Save dialog closed, the app's main window foreground and idle with no frontend export error; the requested, adjacent `.zip.zip`, and two matching Documents paths were absent. The old app's initial suggested name in Documents was not observed, so a cancelled selection versus an unobserved path or other cause remains unresolved. All 55 original files are archived with rechecked sizes and SHA-256 hashes and reviewed for public safety. A bounded harness trial now types the fixture path and activates the focused Save button with physical keystrokes, after path-syntax, exact field, process, dialog, foreground and no-overwrite checks. Timeout diagnosis adds only the suggested Documents name; alternate files still cannot pass qualification. Local Windows contracts pass 30/30 in PowerShell 5.1 and 7, with Biome and whitespace checks passing. Hosted export, normal close and later release gates remain open; audit status stays 33/35.
- **Hosted preflight on `eedc18c`:** [Windows run 35951478822](https://github.com/rndaom/execs/actions/runs/35951478822) stopped at one of 30 contract tests before the package probe: the pure SendKeys path validator refused the test's ambient `os.tmpdir()` fixture export path. That path was not printed, so the exact offending character is unproven; a Windows short-path `~` alias is consistent with an earlier hosted runner path and is intentionally refused by production input. The other 29 contracts passed, and the run produced no artifact. The test now supplies a fixed, representative safe hosted destination and explicitly verifies rejection of `RUNNER~1`; the production validator and Save guards are unchanged. All 30 local Windows contracts, Biome and whitespace checks pass. Physical Save input and ZIP/close qualification still require a hosted package run; audit status remains 33/35.
- **Hosted follow-up on `065f04c`:** The [Windows previous-public capability run 35951761687](../../design/2026-09-22-overhaul/implementation/package-smoke/run-35951761687-windows/README.md) passed 30 hosted preflight contracts, reached the owned native Save dialog and physically entered the exact ZIP path. The Save control was visible and enabled, but UI Automation `SetFocus()` did not give it keyboard focus; the helper stopped **before Enter, ZIP verification or normal close**. All 52 original artifact files are retained with independently verified sizes/SHA-256 hashes and a public-safety review. A bounded follow-up refocuses the proven filename edit and permits Enter only when Windows `DM_GETDEFID` reports Save id `1` as the dialog default, after fresh process, unique foreground dialog, field, control, containment and no-overwrite checks. The query is read-only and timeout bounded. Local Windows contracts pass 31/31 in both PowerShell editions, including wrong-default and lost-focus refusals; native parsing, interop compilation, Biome and whitespace checks pass. A hosted export/close run remains required; the audit tally stays 33/35 and D7/D8 remain open.
- **Hosted follow-up on `aa96fe8`:** The [Windows previous-public capability run 35952618631](../../design/2026-09-22-overhaul/implementation/package-smoke/run-35952618631-windows/README.md) passed 31 hosted preflight contracts and physically entered the exact ZIP path in the uniquely identified v0.1.8 Save dialog. The screenshot shows keyboard focus on `Save as type: Zip` after Tab; UI Automation `SetFocus()` on the filename edit did not regain focus, so the helper stopped **before its default-button query, Enter, ZIP verification or normal close**. The prior observation did not record actual focused-element or default-button values. All 52 original artifact files are archived with rechecked sizes/SHA-256 hashes and a public-safety review. The next bounded trial verifies actual UI Automation focus in the exact Save-as-type control, sends one physical Shift+Tab, polls for focus in the exact filename edit, and repeats process/dialog/foreground, exact field, default Save id `1`, containment and no-overwrite guards immediately before Enter. An unexpected focus or dialog state fails closed; exact ZIP remains the sole success condition. Local Windows contracts pass 31/31, including a PowerShell 5.1 UI Automation focus-ancestry fixture; native parsing, Biome and whitespace checks pass. Hosted export and close remain unverified; the audit tally stays 33/35 and D7/D8 remain open.
- **Hosted pass on `b13d24e`:** The [Windows previous-public capability run 35953614432](../../design/2026-09-22-overhaul/implementation/package-smoke/run-35953614432-windows/README.md) passed all 31 hosted preflight contracts and the authentic public v0.1.8 NSIS install, production WebView2 input, exact native profile export and normal app close in the disposable fixture. The Save receipt records the unique same-PID foreground dialog, exact filename/focus and `DM_GETDEFID=0x534B0001` (default Save id `1`). The retained ZIP is 1,674 bytes, SHA-256 `6f93d339d2c9cfdafaad7f3c754fbbe74b87a1023cd832a70802b429bc261bd2`; all five member hashes and four source payload byte comparisons pass. The runner observed exit code `0` before cleanup without forcing the app, and its before/after/final app-data and live TF2 hash inventories match. All 55 original artifact files are archived with independently rechecked size/SHA-256 provenance and public-safety review. This closes the **previous-public Windows capability subcheck**, while candidate 0.2.0 NSIS install, upgrade/import/reopen, signed updater, standard-user Windows, retail TF2 and Steam Cloud qualification remain open. The audit tally stays 33/35; D7 and D8 remain unchecked.

- **A1/A2:** A switch now refuses still-installed Keep packs before Remove and offers capture into the current profile; fixtures cover unique-name carryover and same-path replacement with live/index bytes unchanged on refusal. Active-profile deletion with Keep installed stores a durable handoff marker, so another profile cannot switch in until Save current as… captures the retained setup. Core unit and integration suites passed (738 unit tests, seven existing ignored cases; 18 absorb and 11 pack-identity tests).
- **A3:** Viewmodel import enumerates bounded VPK members and refuses unrelated content before writes. A model plus `cfg/autoexec.cfg` fixture leaves the live and library VPKs unchanged. 21 viewmodel tests, 19 VPK tests, core check/Clippy, formatting, and diff checks passed. Retail mount behavior remains a release qualification item.
- **A6:** Native ZIP import binds crosshair material/script, viewmodel VPK, and hitsound slot records to manifest files whose hashes were already verified. Three crafted metadata-only ZIPs refuse publication; the full ZIP test group passed (41 passed, one environment-dependent case ignored).
- **A7:** The native sound command journals scoped CFG, autoexec, WAVs, and the record together. Controlled invalid-WAV and manifest-commit failures preserved old bytes and metadata; focused Rust and frontend tests passed.
- **A8:** Current setup creation now verifies the saved active config, reads bounded live `tf/cfg/config.cfg` under the write gate, and copies recent live edits into the new profile. A switch later absorbs that drift into the former active profile. All 14 wizard unit tests passed, including a recent-live-edit fixture.
- **A4/B5:** Managed Binds edits retain unrelated aliases, commands, and comments; the pane lists all inferred startup keys and their sources and makes replacement versus adding explicit. Focused desktop tests (54) and cfglint tests (170) passed. Class/Launch/runtime overrides are tracked separately under B4.
- **A5:** Accepted external drift of managed crosshair, viewmodel, and hitsound payloads marks their saved records `sourceChanged` and each pane warns that the old label may be inaccurate. A Files edit to the Gameplay cfg marks crosshair provenance only when a crosshair CVar changes; an FOV-only edit does not. Focused fixtures and the full core suite passed. Legacy records without an earlier payload fingerprint cannot be retroactively verified.
- **B1:** Unknown external `cl_crosshair_file` values survive color, size, and Gameplay edits and remain visible as external values. Focused frontend tests and the native custom-to-stock roundtrip passed.
- **B4:** Binds, Gameplay, Crosshair, and Sounds now show a read-only notice for matching saved Launch `+` commands and inspected class CFG lines, with links to the source and no extra write gate. 88 focused frontend tests, including a host integration case, passed. Launch timing and later in-game commands remain runtime qualification limits.
- **B11:** Sounds lists other installed packs containing canonical hit/kill paths through the bounded shared content index and labels precedence as conditional. 27 hitsound Rust and 44 focused frontend tests passed; exact retail playback remains qualification work.
- **C6:** Custom Crosshair Build refuses unreadable scripts and requested weapon overrides that were not built before touching live files. Focused Rust/frontend tests passed.
- **B2:** Build, Mod import, and profile switch preflight refuse case-insensitive weapon-script collisions with loose packs and VPKs before live writes. Successful Build records a SHA-256 of the Valve weapon scripts it used; Crosshair warns when those sources change, cannot be read, or predate the hash. The focused Crosshair (31), Mods (28), source-index (3), host, and frontend tests plus the switch no-write fixture pass. Retail mount behavior remains qualification work.
- **B3:** Crosshair now discloses a selected HUD overlay glyph source and installed stock-art candidates, with routes to HUD/Mods and conditional language. The shared content-index and Crosshair tests passed (29 Rust, 44 frontend; one installed-game Rust case ignored), plus host wiring tests. Retail drawing order remains qualification work.
- **B6/C4:** Viewmodels shows effective global Draw state with a route to Gameplay, and shared preload status with a route to its single Casual setup control. Build/import waits for that status to load. Focused Viewmodel tests passed.
- **B7:** Accepting an external loose folder or VPK now creates a stable `External` Mod record linked to the exact pack name. The record survives file drift, profile export/import, and switching, and can be removed through Custom packs. The native round-trip fixture and frontend Mods checks passed.
- **B8/B9/B10:** CFG inventory failures keep same-profile data visibly stale, block only dependent CFG writes, and show the offending path plus retry/Files review; unrelated Comfig/Launch remain available. A failed profile load does not display prior-profile values under the new name. Native detail exposes the selected HUD root so retained inactive roots stay out of CFG inference. Focused SettingsHost/cfg-state tests passed, including the prior failure cases.
- **C4/C5/C7/C8:** The preload owner is Casual setup; shared transparent-viewmodel guidance and Comfig ownership appear across wizard/Comfig/Gameplay; duplicate Comfig choices are named “Use preset” and “Module default”; and Mods presents its installed inventory as “Custom packs.” Focused pane tests passed. The desktop suite passed 963 tests in 105 files at this checkpoint; the integrated local gates are recorded above. Current-head hosted and release qualification remain separate.
- **C1/C2:** The pinned HUD Editor CrosshairStyles palette now edits supported HUD overlay glyphs as a separate source. Unsupported `Special`, `Pulse`, `Shadow`, and unknown operations remain visible but unavailable, with saved values retained. Ten focused core compatibility tests, both pinned corpus tests, 46 HUD frontend tests, and a TypeScript check passed. The rayshud background no longer looks editable when it would do nothing.
- **C3:** The pinned archive/schema harness confirmed all 52 e.v.e Plus alternate choices cause an intended file or CFG effect, so that HUD is enabled. BerryHUD, HExHUD, Community HUD Fixes, and SunsetHUD remain gated with specific tested compatibility reasons rather than silent controls. The corpus harness and schema gate tests passed; these four HUDs remain compatibility limits.
- **D2:** HUD resource artwork URLs now use the same tree SHA as catalog JSON; old moving-`main` cache entries are rewritten in memory on load. A native cache migration test passed.
- **D3:** GameBanana GUI results now include root 1644; HUD subcategory 1649 routes to HUD and other GUI entries to author-guided import. Native Mods install refuses GUI items by name or root ID. Verified against the official categories/index endpoints; 18 native GameBanana and 21 focused frontend tests passed.
- **D4:** Browse lists bounded author download variants with name, description, and size; installation requires a supported file choice, and native rechecks the selected ID and exact download URL. HUD catalog downloads only an unambiguous single ZIP/7z, routing other pages to manual import. Verified against the official download-page API; the GameBanana native, frontend, typecheck, and Clippy checks passed. The HUD catalog has no integrated variant picker.
- **D5:** Showcase albums expire after 24 hours and have a manual Refresh pictures action that bypasses cache. The album age test, bridge/preview command path, and HUD interaction test passed.
- **D7 (open):** The [asset-rights register](d7-asset-rights.md) records the pinned CompVMInstaller, Venom, TF2Hitsounds, comfig.app hosted sound and cueki mod-library sources, how execs uses them, and the permission evidence still needed. `THIRD_PARTY.md` records the known upstream program licenses and unanswered outreach without treating those licenses as grants for every separately authored asset. Venom credits other texture creators, and the [pinned TF2Hitsounds README](https://github.com/WishingStardust/TF2Hitsounds/blob/f5fea33b36931b9bf908de45d8f725530910457f/README.md) attributes some clips to commercial games, so a collection-level reply would not by itself document rights for those clips. Repository pins, runtime downloads and credits were checked, but no per-asset grants are documented. This finding needs rights evidence, rights-cleared replacements, or an owner-approved scope change; the existing 0.1.1 release disposition is not an author grant.
- **D7 September 24 follow-up (open):** Current GameBanana author-license pages allow direct download/install of six unique mods in cueki's source map, but several request permission before redistribution; the pinned bundle has seven addons with no mapped origin. Entry-hash comparisons against two current author files found nonidentical payloads, so direct fetching is not a drop-in replacement for saved selections. The [read-only MDL probe](d7-stock-mdl-probe.md) parsed nine installed class models with 862 animation descriptors, 748 sequences and valid local references; nine synthetic bounds tests passed. The [mapping study](d7-stock-mapping-study.md) identifies item activity and profile-compatibility work still required. Neither research path changes product behavior or checks D7.
- **D7 Scout scratch experiment (open):** An [audit-only root-position probe](d7-scout-root-offset-probe.md) changed six bytes for one Scout draw animation in a hash-pinned scratch MDL, with all other bytes unchanged. Independent readback confirmed the two 439,268-byte hashes and exact changed offsets; the metadata parser still accepts the result. All 17 combined synthetic probe tests and `pnpm check` passed. No model viewer, retail TF2, full group mapping, product pack or export-rights check was completed, so D7 remains unchecked.
- **Adjacent Crosshair compatibility fix:** A valid imported profile fixture with legacy `circle` or `dot` VTF entries previously lost the selected preview and could drop the stored VTF on an unrelated rebuild after the namespaced UI migration. Seeding now remaps only selections backed by those library entries, and core reads the corresponding saved VTF only when the legacy record explicitly names it. A native export/import/rebuild regression preserves the bytes; 32 focused Rust Crosshair tests and 33 focused frontend tests passed. This does not resolve D7's asset-rights evidence.
- **D8 earlier reqwest baseline (historical):** The native reqwest resolver rejected private or reserved answers for approved download hosts at direct connection time, after the bounded precheck. It delegated other hostnames so configured local/private proxy hosts remained usable. A local DNS-change fixture proved direct rebinding to loopback opened no socket; a fake CONNECT proxy fixture proved `localhost` remained reachable and received the destination hostname. The original 22 focused network tests, the full Rust workspace, Clippy, and the ignored live GitHub release-metadata smoke passed. A subsequent 23-test focused run also passed, including authenticated HTTP CONNECT and NO_PROXY regression fixtures. [Reqwest's DNS overrides](https://docs.rs/reqwest/0.12.28/reqwest/blocking/struct.ClientBuilder.html) affected the socket connector; [reqwest 0.12.28's proxy connector](https://github.com/seanmonstar/reqwest/blob/v0.12.28/src/connect.rs) passed the request destination to [Hyper-util's CONNECT tunnel](https://docs.rs/hyper-util/0.1.20/src/hyper_util/client/legacy/connect/proxy/tunnel.rs.html), then used that same destination's hostname for TLS. The proxy resolved the CONNECT hostname independently. The public connector layer could not separately replace the CONNECT authority while retaining the original TLS server name. This was the gap that led to the new transport; no live CDN redirect was exercised by these earlier fixtures.
- **D8 September 24 direct-address and transport follow-up:** The connection resolver also rejects IANA non-global IPv4 `192.88.99.2`, IPv4-compatible/mapped IPv6, local-use NAT64, 6to4, Teredo, documentation, returned and other reserved ranges. Public IPv4, ordinary global IPv6 and the well-known NAT64 prefix with a public embedded IPv4 remain allowed. A 55-address table and 24 focused network tests passed on the direct-address change. The [proxy binding design](d8-proxy-binding-design.md), [CLI loopback probe](d8-curl-probe.py) and [audit-local Rust binding prototype](d8-rust-curl-prototype/README.md) established the two-name transport before product integration.
- **D8 product transport checkpoint (local evidence):** Draft commits `fbc17f2` and `b2cce06` replace reqwest native transfers with a shared curl client and add deadline/redirect regressions. Each approved hop resolves public addresses and connects or tunnels to a vetted numeric address, preserving the original hostname for origin TLS and HTTP; a bounded handle pool is keyed by hostname, address and proxy environment. Redirects, deadlines and byte caps remain explicit. The [product loopback fixture](../../../tools/d8-product-proxy-probe.py) passed 18 Windows cases: authenticated HTTP/HTTPS and SOCKS5h proxy targets, IPv4/IPv6, original SNI/Host, wrong-name certificates, 407, `HTTPS_PROXY`/`NO_PROXY`, redirect rebinding/refusal including a private second-hop answer, body caps, proxy/origin stalls, and same-host tunnel reuse. Independent review found no unresolved high-severity binding flaw. Full Rust workspace tests, Clippy, formatting, packaged-notice check and Biome passed locally; generated notices now include pinned libcurl and bundled zlib C licenses. The hosted results and remaining D8 boundary follow below. Live CDN redirects remain a separate release qualification check.
- **D8 hosted checkpoint on `a9dc1a4`:** All five [CI jobs](https://github.com/rndaom/execs/actions/runs/35960302999), [native Linux](https://github.com/rndaom/execs/actions/runs/35960302997), [unsigned Linux packages](../../design/2026-09-22-overhaul/implementation/package-smoke/run-35960303004/README.md) and the [Windows previous-public capability](https://github.com/rndaom/execs/actions/runs/35960302998) passed on the same PR head. The mandatory Linux product proxy step passed all 18 loopback cases. Linux packages passed AppImage upgrade, Debian upgrade and Debian first install, with 45 public-reviewed fixture files archived. An isolated unsigned Windows 0.2.0 NSIS candidate built at `a9dc1a4` (SHA-256 `2A822318F6041097FA37755CF18EA0BC5EC20706FE9FCDA878A476883EB0C2E4`); generated NSIS instructions carry source-matching curl/zlib notices, and its binary opened and closed normally with isolated app data. That installer was not installed. A later runtime inventory was needed to verify Linux dependency declarations.
- **D8 package correction and closure on `b6a3ca8`:** The [first dependency inventory](https://github.com/rndaom/execs/actions/runs/35961809850) showed direct `libssl.so.3`, `libcrypto.so.3` and `libz.so.1` ELF requirements while the generated Debian `Depends` listed only WebKit/GTK. The draft now declares `libssl3 | libssl3t64` and `zlib1g` and asserts both new and baseline dependencies. On the corrected head, all five [CI jobs](https://github.com/rndaom/execs/actions/runs/35963148612), [native Linux](https://github.com/rndaom/execs/actions/runs/35963148622), [unsigned Linux packages](../../design/2026-09-22-overhaul/implementation/package-smoke/run-35963148670/README.md) and the [Windows previous-public capability](https://github.com/rndaom/execs/actions/runs/35963148614) passed. The generated Debian field exactly matches the declared requirements; AppImage bundles OpenSSL but relies on host zlib, a documented [AppImage base-library assumption](https://docs.appimage.org/introduction/concepts.html). Its AppImage upgrade, Debian upgrade and Debian first-install native round trips passed. This checks the scoped D8 connection-binding finding. Live CDN redirects, installed Windows candidate upgrade, signed updater, Steam Cloud and retail TF2 remain release qualification, not results of these fixtures.
- **D6:** Updated the Inventory development README to match the active-pane refresh/backoff and installed-metadata behavior in the current code. Documentation change checked against the pane and project guide.
- **D1:** Embedded guide navigation now requires HTTPS on the two exact intended hosts and denies separate window requests. This follows Tauri's distinct [navigation and new-window hooks](https://docs.rs/tauri/2.11.5/tauri/webview/struct.WebviewWindowBuilder.html). The native crate suite passed (120 tests, four existing live-network cases ignored); a packaged native click check remains release qualification.
