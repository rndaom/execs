# D7 — Valve asset transformation and profile export

Status: **closed by the owner's September 25, 2026 containment decision** (see the [rights register](d7-asset-rights.md)). Locally built Viewmodels exports carry a recipe that the recipient rebuilds from their own install, not transformed model bytes. The original review follows. This is a source and product-path review, not a legal conclusion or a permission grant. The owner confirmed the Viewmodels builder is required for 0.2.0. The replacement derives models from the player's installed TF2 files; the earlier CompVMInstaller path is disabled in the current development branch while that replacement is built.

## Primary-source boundary

- Valve's [Steamworks Source mod guidance](https://partner.steamgames.com/doc/sdk/uploading/distributing_source_engine?l=english) describes free, SDK-based, clearly identified community mods. Its **Team Fortress 2 Mods FAQ** gives the narrower rule for TF2: a mod may redistribute its own game files, but must load TF2 content from a local install rather than redistribute TF2 game files. The general Source mod distribution paragraph does not settle redistribution of a transformed installed TF2 model.
- The [Steam Subscriber Agreement, sections 2.C, 2.D and 2.G](https://store.steampowered.com/agreement/?l=english) permits noncommercial distribution of content created with Valve Developer Tools, subject to tool-specific terms, and covers Valve game content incorporated into noncommercial fan art, while otherwise restricting copying, modification, derivative works and distribution of Steam content except where permitted. The Fan Art clause names fan art, fiction and audiovisual works; it does not classify a generated TF2 viewmodel VPK or transformed installed MDL as fan art. The [Source 1 SDK license](https://github.com/ValveSoftware/source-sdk-2013/blob/master/LICENSE) allows free modified games subject to the Subscriber Agreement. These sources do not expressly classify an MDL made by changing bytes of an installed TF2 MDL as the mod author's own redistributable file, nor do they expressly resolve a private, same-player backup.
- A copied, locally transformed MDL used by its own player is a different act from sending the result to someone else. These sources do not expressly evaluate execs' proposed binary patcher as an official SDK-based mod, so local transformation also needs a scoped rights review; the TF2 redistribution answer cannot be replaced by an inference from personal use.

## Current execs path

1. The [earlier builder at `e8f759b`](https://github.com/rndaom/execs/blob/e8f759b8297c5946212d91839ea5ce45beb96510/apps/desktop/src-tauri/core/src/viewmodel_build.rs) compiled CompVMInstaller class animation MDLs into a VPK. Fresh builds and source downloads are retired. A possible stock-MDL builder would instead start from installed Valve model bytes. Building and installing that output locally would keep those bytes out of this repository and the execs application download, but would not itself settle later copying or sharing.
2. [Viewmodel install](../../../apps/desktop/src-tauri/core/src/viewmodel.rs) stores `tf/custom/execs-viewmodels.vpk` as a profile file. [The record](../../../apps/desktop/src-tauri/core/src/profile.rs) has `source: compiled | imported`, `sourceChanged`, and an options map; it does not identify a future stock source and exact TF2 revision. Older `compiled` records refer to the retired CompVMInstaller path, not stock provenance; their VPKs remain usable.
3. [Profile export](../../../apps/desktop/src-tauri/core/src/zip.rs) includes every validated manifest file and copies each verified exclusive file into `files/<path>` in the ZIP. That includes `tf/custom/execs-viewmodels.vpk`. The revision-bound export review names included custom packs and flags Viewmodels VPKs and Crosshair scripts before copying. The current import validator requires the VPK whenever a viewmodel record exists, and import retains the payload bytes. Export is user initiated, but the same ZIP serves backup and sharing; the review does not create a separate share-safe path.

The generic export path also matters for other player-install sources:

| Source path | What the current product actually exports |
| --- | --- |
| [Crosshair weapon scripts](../../../apps/desktop/src-tauri/core/src/crosshair.rs) | When the custom Crosshair pack is built, `load_weapon_scripts` reads the player's `tf2_misc_dir.vpk`, patches those scripts, and stores them under `tf/custom/execs-crosshairs/scripts/`. If that pack belongs to the exported profile, the generic ZIP writer copies its patched script files. Their installed-game origin warrants a separate rights review. |
| [Stock hit and kill sounds](../../../apps/desktop/src-tauri/core/src/hitsound.rs) | `extract_stock_hitsounds` reads WAVs from the player's `tf2_sound_misc_dir.vpk` for preview. [The native selection path](../../../apps/desktop/src-tauri/src/commands/hitsound.rs) explicitly refuses `HitsoundPick::Stock` as a file install: the effect is selected with a cvar. Thus a normal stock-effect selection does **not** put a stock WAV in the profile ZIP. An independently imported WAV would be copied if installed, but this code does not establish its origin. |
| [Direct Flat Textures Casual selection](../../../apps/desktop/src-tauri/core/src/preloader/flat_textures.rs) | The author ZIP stays in the app-data cache and the generated `execs-preloader.vpk` is install-wide, outside ordinary profile inventory. The [real-author export/import fixture](../../../apps/desktop/src-tauri/core/src/preloader/tests.rs) checks that neither file is in a profile ZIP; only the saved selection is imported, and the recipient must obtain the source to apply it. This is a verified distinction from generic custom-pack exports, not a permission conclusion for local use or for a separately imported pack with the same textures. |
| [Direct Developer Textures Casual selection](../../../apps/desktop/src-tauri/core/src/preloader/developer_textures.rs) | The original 7z remains in the app-data cache, its contained VPK is parsed in memory, and the generated `execs-preloader.vpk` is install-wide. The [real-author switch/export/import fixture](../../../apps/desktop/src-tauri/core/src/preloader/tests.rs) checks that a profile ZIP carries the saved selection without the author archive or generated pack; applying it on another install requires the exact original author file. This does not establish permission for local transformations or generic exports containing similar game files. |

## Design constraint before a stock builder ships

A shareable profile ZIP must not silently carry transformed installed TF2 model bytes on the assumption that local build permission implies redistribution permission. A candidate design is to export a bounded build recipe and source identity, then regenerate the VPK from the recipient's own verified TF2 install during import or activation. It must explicitly review unavailable or changed source revisions, unsupported hosts, and legacy `yttrium-1` selections; preserve existing approved profile files and imported VPKs without silently dropping them; and prove switch, export/import, and recovery behavior. The current import requirement and Windows-only builder make this a real product change, not a ZIP filter. A separate private-backup policy or an express Valve permission may be needed; the primary sources above do not resolve that case. Review the other player-install-derived export payloads separately before claiming D7 is rights-cleared.
