[package]
name = "execs-inventory-probe"
version = "0.1.0"
edition = "2021"
rust-version = "1.93"
publish = false

# Deliberately outside the desktop release workspace.
[workspace]

[dependencies]
libloading = "0.8"
prost = "0.13"
sysinfo = "0.33"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
