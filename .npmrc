# Hoisted layout: real directories instead of symlinks. Required on Windows —
# Next's standalone build (OpenNext) replicates symlinks, which Windows blocks
# without Developer Mode. CI (Linux) works either way.
node-linker=hoisted
fund=false
audit=false
