# Hoisted layout: real directories instead of symlinks. Required on Windows for
# this monorepo (symlinked node_modules trees need Developer Mode). CI (Linux)
# works either way. See AGENTS.md.
node-linker=hoisted
fund=false
audit=false
