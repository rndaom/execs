# execs — agent notes

Working agreement: durable product/design decisions get recorded here as they are made.

## Product identity
- Name "execs" is a working name — renameable before launch.
- The product is a **Windows + Linux desktop companion** (Tauri 2 + existing React/TS UI, `cfglint` as a library). Not a website.
- The Cloudflare community hub (Steam login, browse, upload, R2, D1) is **parked**. Do not build hub features in parallel.
- V1 audience: TF2 players setting up a fresh install or switching named setups. No Steam login required.
- Everything we ship is **first-party** except mastercomfig (integrated, not rebuilt). Do not embed other community apps.

## Integrity line
- Default surface is file-safe: write `tf/custom/`, `tf/cfg/overrides/` (or vanilla `tf/cfg` user files if comfig is absent), and TF2's Steam Cloud `config.cfg` copy.
- **Preloader exception (user decision, 2026-08-31):** the earlier blanket ban on `gameinfo.txt` edits and official-VPK patching is revoked *for the preloader feature only*. The user researched it and wants the full cueki-style preloader shipped, including its default mod library. Allowed, under hard conditions:
  - `gameinfo.txt`: only the reversible comment toggle on the `type multiplayer_only` line (`type` ↔ `//type`), byte-preserving everything else, pristine copy backed up to app data before the first edit.
  - Official VPKs: only **in-place, size-preserving** patches of `particles/*.pcf` DATA in the sibling archives (pad shrunk files with spaces, refuse larger). **The `_dir.vpk` file is never written — not even the entry CRC.** Hard-learned (2026-08-31 field incident): the directory carries Valve's tree checksum + signature and sv_pure validates against the directory's stock CRCs; rewriting a CRC breaks the tree checksum and the engine rejects the ENTIRE tf2_misc archive on pure servers (every model ERRORs, every material goes magenta). The stale stock CRC over modded data IS the bypass — cueki's data-only patching is deliberate, not sloppy. Tests assert the `_dir.vpk` stays byte-identical through patch and revert. Every entry is **snapshot-first** into app data; "Restore stock files" reverts everything; a RESIZED VPK (TF2 update) invalidates snapshots — mtime drift alone never does.
  - Still absolute: never write `tf/steam.inf`, never `tf/cfg/user/`, never any of this while the game runs, never store `+quit`/`-autoconfig`-style launch damage.
- The preloader lives in `core/src/preloader.rs` + `core/src/pcf.rs` (clean-room PCF/DMX engine — parse, shrink passes, rebuild; cross-validated byte-for-byte against cueki's pipeline on the full default library, with cueki's python-hash-collision merges deliberately not reproduced). Default mods come from cueki's pinned `mods.zip` release (v1.7.1, sha256-verified, cached). Behavior parity kept: blood_trail→npc_fx rename, `_dx80` twins, disguise.pcf material-swap-only rule, duplicate-carrier rebuilds (item_fx/halloween/bigboom/dirty_explode; keep-lists derived at runtime from the user's own vanilla files, so TF2 updates can't strand us on a stale map), `$ignorez` scrub of packed VMTs. Addon content packs into a single global `tf/custom/execs-preloader.vpk` (not profile-owned; profile switches leave it alone; revert removes it). **Enforced** by `GLOBAL_CUSTOM_FILES` in `core/src/surface.rs`, which keeps it out of the live profile surface entirely: otherwise absorb prompts to adopt it after every install, and adopting it means the next profile switch deletes the pack while the global particle patches stay — particles then point at materials that no longer exist (magenta effects). A drift-guard test asserts the exclusion names the same file the writer does.
- First-party Casual preload (RND-164) is a listen-map precache, now cueki-parity: `sv_pure -1; sv_allow_point_servercommand always; map itemtest; wait 10; disconnect; wait 1; clear; script_execute randommenumusic` (`playmenumusic` is not a command — the menu music is a VScript). Vanilla profiles use `+exec execs_preload`; mastercomfig profiles use `+exec overrides/execs_preload`. The cfg is shared by viewmodel packs and the mods preloader: installing mods ensures it for the active profile; full revert removes it unless a viewmodel pack still uses it. Remade in-house — do not vendor cueki/Pilso/Horsie code (GPL): behavior only, clean-room Rust.
- Building viewmodel packs runs TF2's own `bin/studiomdl.exe` with `-game` pointed at an **isolated staging root** (minimal gameinfo.txt under app data) — never the live `tf/` tree. Verified working against a real install; output `.mdl` is read from staging and packed with our own VPK writer (Valve's `vpk.exe` is not used).
- Skins/models/particles in `tf/custom/` may be stored and switched. On Valve Casual they survive only with the preload (and the gameinfo bypass for materials/models). Community/listen servers need neither. Do not claim otherwise.
- Never write `tf/cfg/user/` (removed in mastercomfig 9.9.3). Never write `tf/steam.inf`.

## Profiles
- A profile is the **entire file-safe customization surface**: user cfg layer, `tf/cfg/config.cfg`, **all of `tf/custom/`**, and a launch-options string.
- Switching is **exact replace**, not merge. Inactive profiles live in app data, never under `tf/custom/` (TF2 would mount them).
- Library: Windows `%AppData%\execs\profiles`, Linux `~/.local/share/execs/profiles`. Live game folder holds only the **active** set.
- Export/import is a zip. `mastercomfig-base.vpk` may be shared **by hash** across profiles; everything else is exclusive.
- App can stay open while TF2 runs. **No writes** while `tf_win64.exe` / `tf_linux64` is running.
- After TF2 quits: owned-file / `config.cfg` drift **absorbs automatically** into the active profile. New or deleted packs in `tf/custom/` **prompt, default Update**. Never silently roll back to an old snapshot.
- Steam Cloud stays on. On absorb and on switch, write the same `config.cfg` bytes to `tf/cfg/config.cfg` and `userdata/<id>/440/remote/cfg/config.cfg`. Do not delete Cloud files (Steam redownloads them).
- Progress UI shows real steps (game closed → pack current → remove live packs → write files → Cloud → done), never invented ones. The presenter paces genuinely-reported steps at a minimum display duration (`SWITCH_STEP_MIN_MS`) and fills a step-driven bar so a milliseconds-fast switch still reads as a sequence; no percentages, no steps marked done before their event.

## First run and new profiles
- Find TF2 by scanning Steam libraries (registry, `libraryfolders.vdf`, `~/.local/share/Steam`, `~/.steam/steam`, Flatpak data dir). Confirm `tf/steam.inf` app `440`. Always offer Browse. Multiple installs → picker. Remember one root; profiles are tied to it. No silent write until the path is confirmed.
- **Existing customization:** first launch only **Save current as…**. Do not install comfig on top until they ask.
- **Unused install:** first launch is the setup wizard.
- **Create new profile** (after they already have one) is when the wizard runs for existing users.
- A new profile is a **fresh TF2**: stock binds from Valve `config_default.cfg`, wizard comfig layer, empty `tf/custom/` except official VPKs they picked. Different profile = different binds.
- **Start from (user decision, 2026-09-01):** Create-new-profile opens with two tiles above the name field. **Current setup** (default when a profile is active) copies the active profile's `tf/cfg/config.cfg` **verbatim** — binds, every archived cvar (audio, `con_enable`, `hud_fastswitch`, advanced options) and the `tf_training_has_prompted_*` / `tf_explanations_*` "already shown" flags — so a new profile is seamless and pops no tutorial dialogs. **Fresh TF2** is Valve's `config_default.cfg`, as if newly installed. Everything else (comfig layer/preset, official addons, HUD, `tf/custom/`) always comes from the wizard picks. The first-run wizard has no active profile to copy, so it shows no tiles and is always Fresh. The old inherit-binds settings checkbox is superseded and removed.
- Do not run the nuclear clean-up (`delete tf/cfg` + `-autoconfig -default +quit`) as a normal switch or create.

## mastercomfig
- Do not rebuild the VPK core, interp research, class-cfg takeover, or comfig.app's bind/weapon-VTF customizer.
- V1 in-app: **our UI** for preset, modules, addons. Fetch **official GitHub Release** VPKs at install/update time. Write `tf/cfg/overrides/` (`modules.cfg`, `setup_hook.cfg`, etc.).
- Credit + link to https://comfig.app and their support/donate. Do not brand as official mastercomfig. Do not bundle a stale VPK in our repo as "ours."
- Extras and the preset guide open **in-app**: `open_embedded_page` builds a dedicated WebviewWindow (`comfig-extras` → https://comfig.app/app/, `comfig-docs` → https://docs.comfig.app/latest/). Remote pages get no Tauri IPC — never add them to a capability. Import `comfig-custom/` if they used it.
- Every other external link goes through `openExternal` (tauri-plugin-opener) — plain `target="_blank"` anchors are silently swallowed by wry on Windows.
- Preset preview images are the real mastercomfig koth_sawmill screenshots (MIT, comfig-app repo) vendored under `src/assets/presets/`, re-encoded 1280×720. Never ship AI-generated preview art.

## V1 settings UI
- **Comfig** — preset, modules, addons. No "packages installed" status text when everything is fine; only problems (not installed / read-only / saving) get a status pill.
- **Binds** — click-to-record for usual actions (movement, jump/duck, medic, use, voice, loadout). No alias/script studio.
- **Gameplay** — FOV, viewmodels, and obvious toggles. First-party; writes overrides. Also mirrors the transparent-viewmodels **addon** as an instant-apply toggle (comfig-layer profiles with packages only; it is an addon VPK, never a cvar or module — nothing is written to `execs_gameplay.cfg` for it).
- **HUD** — hud-db catalog, pinned GitHub zip install (one HUD mounted), schema options for popular HUDs. First-party apply; do not embed TF2HUD.Editor. Catalog is paginated (`HUD_CATALOG_PAGE_SIZE`) and each entry's hud-db screenshots open in an in-app lightbox (video URLs filtered out). **One-click for every host (2026-09-02):** `HudInstallKind` is derived from the `repo` host — `github` (pinned codeload zip), `direct` (Dropbox share links forced to `dl=1`; all 26 are 7z), `gamebanana` (`apiv11/Mod/{id}/DownloadPage`, newest zip/7z by `_tsDateAdded`), `thread` (teamfortress.tv post scraped for its last Dropbox archive link), `none` (ToonHUD's theme builder, Steam groups, dead links → the row opens the author's page). `extract_hud_archive` sniffs magic: zip, 7z (`sevenz-rust`, decompression only, same entry/size caps), RAR refused by name. The lightbox also loads the author's `social.album` in-app: Imgur `/a/`, `/gallery/` and `i.imgur.com` through `api.imgur.com/3/album/{id}/images` with imgur's own web client id `546c25a59c58ad7` (500/h per IP; results cached forever by URL), and GitHub `blob/…/showcase.md` pages through the raw markdown's image URLs. Catalog cache is `catalog-v3.json`.
- **Crosshair** — default in-game crosshair controls plus the **custom crosshair** builder ("custom crosshairs", not "per-weapon crosshairs"). Sources: first-party shapes, imported PNG, the **parametric designer** (style/size/thickness/gap/dot/outline/shadow/opacity → real 64×64 RGBA, params persisted on the record as `design`), and the **community pack** (Venom Crosshairs, 173 static entries pinned to hbivnm/Venom-Crosshairs-List@2e7036cd, fetched on demand, cached, written into the pack **verbatim** with their own dimensions in the weapon scripts; the 3 animated crossanim_* entries are excluded; credit HbiVnm + community authors). Named library entries live as bytes in the pack itself (manifest stores only name→format); re-apply recovers missing bytes from the pack. The stock live preview renders Valve's REAL sprites decoded from the user's own `tf2_textures_dir.vpk` (`get_stock_crosshair_sprites`, DXT/BGRA decoder in `core/src/vtf_read.rs`), with extracted-geometry SVG as fallback; scale = ×`cl_crosshair_scale`/32; `cl_crosshair_alpha` is stored but the engine draws opaque. Pack colour is **cvar-based**, not baked: textures stay white and apply writes the pack's tint to `cl_crosshair_red/green/blue`, so community VTFs recolour too; weapon overrides offer an "All classes" per-slot mode (picking the base shape clears overrides so the fallback keeps working) and copy-class-to-all fans out to OTHER classes only. Every crosshair is shown as a picture (2026-09-02): the stock file picker is a grid of the decoded Valve sprites, library chips and the weapon-override popover (`CrosshairChoice`, no `<select>`) draw through one `paintCrosshair`, and the community picker fetches all 173 VTFs on open (`fetch_community_crosshair_previews`, 8 workers, ~1.7 MiB, cached by commit — upstream ships no preview images) and tints them with the pane colour. Our VTF writer uses the spec header layout (bumpScale@48, format@52, mips@56 — an older build wrote these 4 bytes early). Default controls keep writing the gameplay override; the builder writes `tf/custom/execs-crosshairs/`.
- **Sounds** (2026-09-02) — hit sound and kill sound. TF2 only plays the canonical `sound/ui/hitsound.wav` / `killsound.wav` (the 2010 `tf_dingaling_*_wav_override` cvars no longer exist; those two names are what the engine exempts from sv_pure on Valve servers), so the pack is exactly `tf/custom/execs-hitsounds/sound/ui/{hitsound,killsound}.wav` (`core/src/hitsound.rs`, manifest `hitsound: { hit, kill }` with name + source). Engine rules: 8/16-bit PCM or 4-bit MS-ADPCM at 11025/22050/44100 Hz, mono or stereo; MP3 renamed to .wav does not play. `prepare_hitsound_wav` passes engine-ready files through verbatim and re-encodes other PCM/float WAVs to 16-bit 44.1 kHz (linear resample, >2 channels folded to stereo); ADPCM at a bad rate and non-WAV containers are refused with the reason. Cvars (`tf_dingalingaling`, `_lasthit`, volumes 0–1, pitch 1–255 at ≤10/≥150 dmg, `_effect`/`_last_effect` 0–8, `_repeat_delay`) ride `execs_gameplay.cfg`; a custom file only plays with effect 0, so picking a built-in effect clears the slot's file. Selection: the nine built-in effects auditioned from the user's own `tf2_sound_misc_dir.vpk` (never redistributed), the community pack (WishingStardust/TF2Hitsounds pinned to f5fea33b, 32 of 33 — `levelup` is 32 728 Hz), and the user's own WAV via the file dialog (prepared once, stashed under app data by a random token, auditioned and installed from there — the frontend never handles a path). Audio bytes cross the bridge as `tauri::ipc::Response`, never JSON arrays. `sound.cache` next to the pack is deleted on every apply (a stale cache plays the previous file).
- **Viewmodels** — **Yttrium-style only** (user decision 2026-08-31): per-class checkbox groups (64 groups generated from CompVMInstaller's source into `core/src/viewmodel_groups.rs` + `lib/viewmodel-groups.ts`, with two upstream bugs fixed — Wrangler installs, dragons_fury_inspect_end included; hiding any Soldier group force-hides the Original like upstream). Build = fetch Yttrium's `animations.zip` (pinned commit b215a5cd, sha256-verified, cached), rewrite chosen SMDs off-screen (`hide_smd_sequence`, frame counts preserved), compile per class with the install's own studiomdl in staging, pack with our VPK writer, install through the import machinery (`ViewmodelSource::Compiled`, `options.hidden` CSV, `options.mode`). **Two hide modes** (`ViewmodelHideMode`, user request 2026-08-31): `full` moves every bone off-screen, so the weapon *and* the arms vanish — CompVMInstaller's behavior and the default, including for packs built before the option existed; `weapon` rewrites only the `weapon_bone*` / `vm_weapon_bone*` rows, which every class parents to `bip_hand_L`/`bip_hand_R`, so the weapon goes and the hands keep animating (all frames preserved, nodes section untouched). Either way a hidden weapon is also hidden in third person — TF2 shares one c_ model between both views; say so in the UI rather than implying first-person-only. Credit Yttrium; assets are fetched from his repo, never vendored. The Horsey-editor concepts (origin/rotate/keep-visible/static/weapon extras, QC/SMD knobs, `viewmodel_compile_capability`) are REMOVED, not hidden — do not reintroduce without a new product decision. Prebuilt VPK import and Casual preload remain. Preview (2026-09-02) is CompVMInstaller's own "visual image guide": one 16:9 in-game screenshot per option (weapon out) and one blank per class (hidden), hover an option to see its current state, click to flip it, class tabs reset to the blank — the 73 JPEGs are fetched on demand from the same pinned commit as `animations.zip` (`viewmodel_fetch::fetch_preview_image`, cached by commit, returned as raw bytes via `tauri::ipc::Response`), credited to yttrium/Oblique, never vendored. The group → image table is `lib/viewmodel-previews.ts`; a test asserts every group has one. The wiki renders under `src/assets/viewmodels/` remain only as the browser-preview fallback. File-safe FOV/min stay on Gameplay.
- **Files** — raw cfg + `cfglint`. Files are classified by origin (user/app vs engine/HUD/pack/comfig-import): non-user files are read-only and lint **advisory-only** (block findings demote to warns via `advisoryPaths`), so provided content can never lock the user's own saves. `tf/cfg/config.cfg` stays strict-with-exemptions and editable.
- **Launch options** — copy-to-clipboard. Auto-write `localconfig.vdf` only if Steam is already closed. Never force-quit Steam to finish setup. Never store `-autoconfig`, `-default`, `-dxlevel`, or `+quit` on a profile.

## Not V1 (profiles still carry these files if already present)
- Sharing site / Steam auth. Later: export zip is enough to start.

## Updates
- The app has an **in-app updater**: GitHub Releases + Tauri updater. Check on launch and via footer **Check for updates**. Show “update available,” they click **Install**. **Later** dismisses for the session. No silent force-replace, no analytics/telemetry.
- Endpoint: `https://github.com/rndaom/execs/releases/latest/download/latest.json` (published, non-prerelease tags only). Sign with GitHub secrets `TAURI_SIGNING_PRIVATE_KEY` and optional `TAURI_SIGNING_PRIVATE_KEY_PASSWORD`. Pubkey lives in `apps/desktop/src-tauri/tauri.conf.json`. Never commit the private key.
- Linux updates are AppImage. `.deb` is first-install only. Windows uses NSIS (`installMode: passive`). No macOS.
- mastercomfig VPK updates are a **separate** control on the Comfig pane (their GitHub releases, not ours).

## Implementation status
- Product grill is **done**. This file is the V1 spec.
- Linear: [execs](https://linear.app/rndaom/project/execs-a89f9a30e95c) (team Rndaom).
- [RND-144](https://linear.app/rndaom/issue/RND-144) scaffold is on `main`. [RND-145](https://linear.app/rndaom/issue/RND-145) / [RND-146](https://linear.app/rndaom/issue/RND-146) finder + write-lock are on `main`.
- [RND-147](https://linear.app/rndaom/issue/RND-147) profile library lives in app data (`profiles/` next to `settings.json`): index + per-file sha256, exclusive trees, `mastercomfig-base.vpk` shared by hash.
- [RND-148](https://linear.app/rndaom/issue/RND-148) save-current snapshots the live file-safe surface (overrides / vanilla cfg, `config.cfg`, all of `tf/custom/`, launch options) into a named profile without deleting live files. First save sets the active profile.
- [RND-150](https://linear.app/rndaom/issue/RND-150) after TF2 quits (and on boot if the game is closed), owned-file / `config.cfg` drift absorbs into the active profile. New or deleted `tf/custom/` packs prompt, default Update. Absorb dual-writes `config.cfg` to the live folder and Steam Cloud. Never rolls the live folder back.
- [RND-149](https://linear.app/rndaom/issue/RND-149) exact-replace switch with a real progress panel (closed → pack → remove → write → Cloud → done). Previous exclusive files are removed only when the live hash still matches. At most one HUD folder is mounted; extras write as `-name`.
- [RND-151](https://linear.app/rndaom/issue/RND-151) zip export/import copies a named library profile to a versioned zip (`execs-profile.json` + exclusive `files/` + hashed `blobs/`). Import creates a new library record, rewrites `tf2_root`, and does not apply to live TF2 or change `activeProfileId`.
- [RND-152](https://linear.app/rndaom/issue/RND-152) first launch classifies the live file-safe surface. Existing customization is Save current as… only (no Import, no comfig install). An unused install is the setup wizard (name + preset + official addons). Apply downloads official mastercomfig GitHub Release VPKs, materializes an inactive library profile, then exact-replace switches it on. No silent write on first sight.
- [RND-153](https://linear.app/rndaom/issue/RND-153) Create new (only after a profile exists) reuses the wizard: stock `config_default.cfg` binds, wizard `setup_hook.cfg`, official VPKs they picked, empty `tf/custom/` otherwise. Apply materializes an inactive profile then `switch_profile` (pack absorbs the active profile first). Superseded 2026-09-01 by the Start-from choice above (RND-187): the wizard's two tiles pick Current setup (verbatim copy of the active profile's `config.cfg`) or Fresh TF2 (stock `config_default.cfg`); the inherit-binds checkbox is gone.
- [RND-154](https://linear.app/rndaom/issue/RND-154) Comfig pane: preset → `setup_hook.cfg`, modules → `modules.cfg`, official addons via GitHub Release VPKs (base shared by hash), Update packages, comfig.app credit/import `comfig-custom/`. Not branded official mastercomfig.
- [RND-155](https://linear.app/rndaom/issue/RND-155) Bind recorder for usual actions writes `execs_binds.cfg` (overrides or vanilla). Autoexec gets the exec line **addressed from tf/cfg** — `exec overrides/execs_binds` on the comfig layer, bare `exec execs_binds` only on vanilla. The engine resolves `exec` relative to each search path's cfg folder, never the exec'ing file; a bare stem inside overrides/ silently fails in game (2026-08-31 field bug — binds never loaded). `ensureAutoexecExecLine` migrates old bare managed lines in place, and cfglint's `resolveExec` mirrors engine resolution (`/cfg/<target>` endings only) so the Files pane flags what the game would drop. After absorb, tracked in-game Options binds sync into that file. No `unbindall`, no alias studio.
- [RND-156](https://linear.app/rndaom/issue/RND-156) Gameplay pane writes `execs_gameplay.cfg`: FOV 54–90, viewmodels, tracers, and flip (not while connected). Default stock crosshair controls live on the Crosshair pane but serialize through this same managed file. Autoexec gets `exec execs_gameplay`.
- [RND-157](https://linear.app/rndaom/issue/RND-157) Files pane: raw cfg editor + live `@execs/cfglint`. Block-tier (`ok === false`) refuses Save; no silent strip. Warn/info are advisory. Only `tf/cfg/config.cfg` is marked engine-managed: its top-level reset, safe ESCAPE menu bind, and archived console preference are accepted; shared/user cfg lint stays strict.
- [RND-158](https://linear.app/rndaom/issue/RND-158) Launch options on the profile manifest; copy-to-clipboard. New/wizard profiles get the official comfig set (`-novid -nojoy -nosteamcontroller -nohltv -particles 1`). Auto-write `localconfig.vdf` only if Steam is closed. Switch uses the same rule. Never store `-autoconfig`, `-default`, `-dxlevel`, `+quit`, or `gamemoderun %command%`.
- [RND-162](https://linear.app/rndaom/issue/RND-162) HUD pane: hud-db catalog (MIT), pinned `codeload` zip install, one-HUD replace/dash, optional `ProfileManifest.hud`. Schema options (rayshud, budhud, flawhud, m0rehud, kbnhud, hypnotize-hud) from TF2HUD.Editor JSON as data; first-party apply (folder swap, VDF merge, `#base`, WriteCfg). Non-GitHub rows are outbound links. Casual copy: layout/scheme usually work; custom materials usually do not.
- [RND-163](https://linear.app/rndaom/issue/RND-163) Crosshair pane: default in-game file/color/scale controls followed by the first-party **custom crosshairs** builder. Procedural shapes plus optional user PNG. Writes `tf/custom/execs-crosshairs/` (`materials/vgui/replay/thumbnails/` + patched `scripts/tf_weapon_*.txt`). Weapon script bodies are read from the user's local `tf2_misc_dir.vpk` (ICE decrypt in-process). Never write official VPKs. Never ship `vtf2tga.exe` or Valve sprites. Applying forces `cl_crosshair_file ""` and writes the pack tint to `cl_crosshair_red/green/blue` (textures stay white — nothing is baked).
- [RND-178](https://linear.app/rndaom/issue/RND-178) Custom-crosshairs follow-up: parametric designer, Venom community pack (173 static entries, pinned + cached), stock preview from the user's own `tf2_textures_dir.vpk` (`vtf_read.rs`), spec-correct VTF writer, cvar-based tint.
- [RND-164](https://linear.app/rndaom/issue/RND-164) Viewmodels pane (superseded 2026-08-31 by RND-179): the Horsey-class compiler was dropped by user decision in favor of Yttrium-style per-group hiding. First-party Casual preload remains (now cueki-parity cfg, shared with the mods preloader).
- [RND-179](https://linear.app/rndaom/issue/RND-179) Yttrium-style viewmodels: 64 per-class checkbox groups, fetch pinned `animations.zip`, rewrite SMDs off-screen, compile with the install's own studiomdl in an isolated staging root, pack with our VPK writer. Horsey-editor knobs removed. Prebuilt VPK import remains.
- [RND-180](https://linear.app/rndaom/issue/RND-180) Cueki-style casual preloader + Mods pane: reversible `gameinfo.txt` comment toggle, in-place size-preserving `particles/*.pcf` patches in `tf2_misc` (snapshot-first, one-click restore), clean-room `pcf.rs`, default mods from cueki's pinned `mods.zip` v1.7.1, global `tf/custom/execs-preloader.vpk`. Never while the game runs.
- [RND-159](https://linear.app/rndaom/issue/RND-159) in-app updater: GitHub Releases + Tauri updater. Check on launch, banner, click to install. Endpoint `latest.json`. Signing via `TAURI_SIGNING_PRIVATE_KEY`. Linux updates are AppImage. No silent replace, no telemetry. mastercomfig VPK updates stay on the Comfig pane.
- [RND-177](https://linear.app/rndaom/issue/RND-177) V1 polish pass: Inter/de-carded surface, real preset and viewmodel imagery, honest paced switch progress, crash hardening, advisory lint on provided files, stock crosshair geometry preview, HUD pagination/lightbox, in-app comfig extras/docs, compiler UI hidden until file-safe.
- [RND-181](https://linear.app/rndaom/issue/RND-181) 2026-09-01 audit follow-up: plan in docs/audit-2026-09-01.md; Phases 0–5 landed (hub off main, CI gates, 14 P0 fixes, core/IPC/frontend refactors, cfglint trust model, Start from current setup).
- [RND-188](https://linear.app/rndaom/issue/RND-188) Phase 6 visual pass: the design system above (tokens, type scale, accent budget, grouped sidebar, one lock indicator, `OnboardingFrame`, hero-row rule). Before/after captures and the full write-up are in `design-qa.md`, 2026-09-01.
- Next: [RND-160](https://linear.app/rndaom/issue/RND-160) Windows code signing and the CI installer build (`release.yml` already builds nsis/appimage/deb, and the release draft/verify gate landed 2026-09-01).

## Design decisions
- **Design system (RND-188, 2026-09-01)** — one system, defined in `apps/desktop/src/index.css` `@theme`; no hardcoded hex anywhere else. Three surfaces: bg `#121212` → panel `#181818` → panel-raised `#1F1F1F`. Two hairlines expressed as ink alpha so they read the same on every surface: `--color-edge` 8%, `--color-edge-strong` 14%. Warm ink `#ECE7DA` / muted / faint.
- **The accent is a budget.** `#CF6A32` is spent on exactly four things — the primary button, the selected option ring, the active nav marker, the wordmark dot. Everything else is ink. Semantic colour (`--color-ok` / `--color-warn` / `--color-error`) carries meaning only; team colours survive only where they name a TF2 team. No TF2-style display face (Big Shoulders removed 2026-08-30).
- **Type scale** — Inter only, six steps as utility classes: `.t-pane` 28/600, `.t-section` 17/600, `.t-row` 15/500, body 14/400 at 1.55, `.t-meta` 12.5/400 muted, `.eyebrow` 11/500 uppercase 0.12em — the only uppercase in the product. `.tnum` on numeric readouts. Copy is sentence case; buttons say what happens.
- **Spacing** — 4px base, 880px content column, 40px gutters, 48px between sections, 44px minimum row height.
- De-carded layout: pages are flat sections separated by hairlines (`.section`); rows use dividers, not per-row boxes. Boxes are reserved for real surfaces (`.surface`: editors, previews, media), overlays (`.overlay`: popover, progress, prompts), and selectable option tiles (`.tile` / `OptionTile`) whose selected state is a 1.5px accent ring plus a small check — never a fill. `.badge` is sentence case with a hairline ring, filled only when semantic. Buttons are `.btn`/`.btn-primary`/`.btn-ghost` (rounded-lg); `rounded-pill` is for badges and toggle knobs only.
- **Hero-row rule** — every settings pane opens with `PaneHeader` (28px title, one-line lede) then a `.hero-row`: the one decision that matters on the left, a fixed 360px `.surface` preview on the right that stacks under the choices below 1100px and never becomes a full-width banner. Secondary depth folds into a `Disclosure` (closed by default, state remembered per pane in localStorage).
- **Onboarding** — FinderPanel, FirstRunExisting and SetupWizard all render through `OnboardingFrame` (wordmark, eyebrow, balanced title, lede ≤62ch, 640px column; 880px for the wizard) so the three screens are one family.
- **One lock indicator** — while TF2 runs, the top `WriteLockBanner` is the only place that says so. No per-pane pill, no sidebar box; the disabled controls carry the rest.
- **Motion budget** — 150ms ease on colour, opacity and ring; movement (a pane or picture arriving, a disclosure opening, a modal) uses `--motion-move` 220ms with `--ease-out` via `.enter-fade` / `.overlay-enter` / `.scrim`; buttons press to 0.985; nothing at all under `prefers-reduced-motion`.
- **Fewer controls (user feedback, 2026-09-02)** — no cards where a row will do, no `<select>` where a picture or a `Segmented` pill will do, no check marks (a selected tile is a ring, a 6% accent wash and an 8px dot), no native checkboxes (switch rows), no inner scroll boxes on a page that can scroll. Sliders are `.range` (ink thumb; only the crosshair RGB sliders keep team-coloured thumbs). Anything that is a picture (crosshairs, HUDs, viewmodels) is chosen by looking at it (`.thumb` grids).
- Not affiliated with Valve / Steam. Credit mastercomfig where we use it. "Powered by Steam" footer is hub-era. The desktop not-affiliated disclaimer exists: the App footer carries the full Valve/Steam line (short form when cramped) and the onboarding surfaces (setup wizard, Comfig pane) carry the mastercomfig/comfig.app one.
- TF2 root is remembered only after Confirm. Settings live at Windows `%AppData%\execs\settings.json` and Linux `~/.local/share/execs/settings.json` (`{ "schema": 1, "tf2Root": "..." }`; a legacy `inheritBinds` key is ignored, not an error). Do not use Tauri's `com.rndaom.execs` app-data dir — RND-147 puts `profiles/` next to this file.
- Finder: Steam roots (Windows `SteamPath` / `InstallPath`, Linux ticket paths plus `~/.steam/root` and lowercase `steam`) → `libraryfolders.vdf` / `appmanifest_440.acf` → accept only after `tf/steam.inf` app `440`. Multiple installs are a picker. Browse always stays visible. A `tf/` pick normalizes to its parent. A stored root that fails validation is unconfirmed.
- Write lock: process **name** `tf_win64.exe` (Windows) / `tf_linux64` (Linux). `refuse_if_running()` wraps live-surface and profile-library writes. Confirming the TF2 root and toggling inherit-binds are app-data settings writes and stay allowed while the game is open. Do not lock on `steam`, `srcds`, `hl2.exe`, or Proton `tf_win64.exe` on Linux.
- Crash hardening: release builds do **not** use `panic = "abort"` (one background-thread panic must not kill the app); a panic hook logs to `<execs data dir>/logs/panic.log`; the lock poller wraps each tick in `catch_unwind`; heavy commands (save/absorb, crosshair apply, HUD match) run in `spawn_blocking`, never on the main thread; the VPK reader range-reads sibling archives and supports filtered reads (`read_vpk_dir_file_filtered`) so weapon-script loading never materializes gigabytes.

## Deployment (legacy hub — parked)
- The hub source is **not on `main`** (removed 2026-09-01, RND-182 Phase 0). `apps/web`, `packages/preview-matrix`, and `tools/capture` live on the `hub` branch, which preserves the full pre-removal tree. Un-parking means reviving that branch, not re-adding files to `main`.
- Old live URL: https://execs.anthonyrandomcarey.workers.dev (Cloudflare Worker "execs"). Do not treat as the product.
- Production D1 / R2 remain from the hub; no new hub migrations unless someone un-parks it.
- `.npmrc` `node-linker=hoisted` — still required on Windows for this monorepo. Don't remove it.

## Conventions
- Plain `package.json` scripts only (Windows dev machine) — no bash-isms.
- LF line endings enforced via .gitattributes; cfg fixtures must never be CRLF.
- `design-qa.md` is a dated QA log; add an entry per visual QA pass.
- Bash heredocs in this environment mangle backslashes and choke on apostrophes; write patch scripts to the scratchpad with the Write tool and run them.
- **Preloader untracked entries (2026-09-02):** the `C_OP_RenderSprites::RenderUnsorted … unimplemented sprite renderer` console flood is the engine's `orientation_type 0` path on a system whose material failed to load — not a PCF bug. It came from Square_Series patches whose tracking was lost in the 2026-09-01 baseline reset (61 modified `tf2_misc` entries, no snapshots, materials no longer shipped). `PreloaderStatus.untracked_modified` lists such files; the pane tells the user to restore, verify game files in Steam, then re-apply. Snapshots record `pristine` (bytes matched the directory's stock CRC) and a resized VPK judges every entry against that CRC instead of discarding tracking.
- **Launching the real game for verification must not change the user's settings.** Never pass video-mode flags (`-w`, `-h`, `-windowed`, `-noborder`, `-fullscreen`, `-dxlevel`): Source persists the mode it ran with into `HKCU\Software\Valve\Source\tf\Settings` (`ScreenWidth`/`ScreenHeight`/`ScreenWindowed`/`ScreenNoBorder`), so the user's resolution stays changed afterwards — hit for real on 2026-08-31. `-condebug` (writes `tf/console.log`) is fine; `-console` is fine but persists `con_enable "1"` into config.cfg, which then absorbs into the active profile. Launch bare, read `console.log` for evidence, and back up + restore anything a test must change.
